# Local secrets and generated TLS material — never commit.
.env
ssl/server.crt
ssl/server.key
ssl/*.pem
ssl/dhparam.pem
ssl/*.tmp

# Per-deployment state: staged ApiSchema workspace + bootstrap-complete sentinel.
.bootstrap/
