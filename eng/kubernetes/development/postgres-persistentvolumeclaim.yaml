apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    serviceName: pgsql-data
  name: pgsql-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
