{
    "$comment": [
        "The published artifacts the stock-image plugin proof runs against, recorded from the",
        "qualifying release dms-pre-8.0.1-alpha.0.155. It was cut from aeb211a3f, and publication",
        "run 35919516675 pushed the Ed-Fi API image and the EdFi.Api.SchemaTools package pinned",
        "below. The contract packages are the EdFi.Api.Plugins 1.0.0 and EdFi.Api.CustomValidation",
        "1.0.0 that DMS-1501 published in run 35688731069. Its Jira comment 99164 records that",
        "publication and the external-consumer restore and compile against the feed. Run",
        "35919516675 found both already published and unchanged, and published no contract bytes.",
        "",
        "The pulled image carries both required runtime features. DMS-1499's loader runs the",
        "load-plugins startup phase that reads /app/plugins, applies the allowlist and registers the",
        "fixture's ICustomResourceValidator. DMS-1433's CustomResourceValidationMiddleware invokes",
        "the registered validators for the request's resource and turns their failures into the",
        "custom-validation 400. Without the loader the validator never reaches DI. Without the",
        "fan-in step it loads and resolves but never runs, so the POST gets a 201 or a generic 400,",
        "and the proof accepts neither.",
        "",
        "The Configuration Service image is the independently published CMS image pinned by digest.",
        "A dms-pre release publishes no CMS image, so it comes from its own release,",
        "cs-pre-8.0.1-alpha.0.154, published by run 35909464192 from 8b43a5fee. That commit is the",
        "parent of aeb211a3f and differs from it in nothing under src/config.",
        "",
        "A release qualifies when all of the following hold. It carries DMS-1499's plugin loader and",
        "DMS-1433's custom-validator fan-in step, because loading and resolving a validator without",
        "invoking it cannot produce the custom-validation 400 this proof asserts. It was cut after",
        "this ticket's version-specific image tagging merged, or there is no version-specific tag to",
        "pin. And it follows DMS-1501's successful publication of EdFi.Api.Plugins and",
        "EdFi.Api.CustomValidation together with that story's external-consumer restore and compile",
        "evidence, because the contracts fields below name published packages this proof restores",
        "rather than builds, and an unverified contract publication would be pinned here unexamined.",
        "",
        "Nothing here may be guessed. A fabricated tag or digest would make the proof a statement",
        "about an artifact nobody published, which is the one thing this tier exists not to be.",
        "Test-StockImagePinReadiness.ps1 refuses a half-filled pin in either direction: a 'pending'",
        "pin carrying values, and a 'published' pin missing any of them, are both invalid rather",
        "than pending.",
        "",
        "edFiApi is pinned by version-specific tag AND digest. configurationService is pinned by",
        "digest alone: this ticket adds version-specific tags to the Ed-Fi API publication only, so",
        "the Configuration Service still carries nothing but the moving 'pre' tag, and a bare digest",
        "is honest about that where a tag plus digest would imply a version-specific tag exists.",
        "",
        "provisioning and contracts pin what the harness consumes rather than builds: the released",
        "SchemaTools package that provisions the database the pinned runtime validates, and the two",
        "published contract packages the fixture plugin compiles against. Building the fixture from",
        "this branch against this branch's contracts would test version skew rather than plugin",
        "loading, and would stop working the moment either contract surface moved. Each of the two",
        "carries its own feed address: the SchemaTools package and the contract packages are separate",
        "publications, and neither may borrow the feed a schema package entry happens to name.",
        "",
        "schemaPackages is the whole of what selects schema content, and dataStandardVersion is only",
        "a label. eng/docker-compose/.env.bootstrap.ds52 sets DMS_CONFIG_DATA_STANDARD_VERSION to",
        "'5.2' and, quite separately, SCHEMA_PACKAGES to exact package identities; a pin carrying",
        "only the label would take whichever versions this branch's overlay happened to name, which",
        "is the current-worktree schema content this proof must not be running against. Each entry",
        "carries name, an exact version, and the feed it came from, so a catalog default or an",
        "ambient feed cannot substitute an input. The harness writes this set as the one",
        "SCHEMA_PACKAGES value in the environment file, which is what both the schema preparation",
        "phase and the container read, so the two cannot disagree."
    ],
    "status": "published",
    "edFiApi": {
        "repository": "edfialliance/ed-fi-api",
        "tag": "8.0.1-alpha.0.155",
        "digest": "sha256:ace0564a0e0d3578319ea8c84d2d0bce163106eacedc064439a650388d8b38f0"
    },
    "configurationService": {
        "repository": "edfialliance/ed-fi-api-configuration-service",
        "digest": "sha256:d9566b364ec43b1fff9298407230cf52ad99d836f5095bf6557b011e811352f5"
    },
    "release": {
        "githubRelease": "dms-pre-8.0.1-alpha.0.155",
        "sourceCommit": "aeb211a3fab6e1719e71a24a9a5df3e4e690c8d4",
        "publicationRunUrl": "https://github.com/Ed-Fi-Alliance-OSS/Data-Management-Service/actions/runs/35919516675"
    },
    "provisioning": {
        "schemaToolsPackageVersion": "8.0.1-alpha.0.155",
        "schemaToolsFeedUrl": "https://pkgs.dev.azure.com/ed-fi-alliance/Ed-Fi-Alliance-OSS/_packaging/EdFi/nuget/v3/index.json",
        "dataStandardVersion": "5.2",
        "schemaPackages": [
            {
                "name": "EdFi.DataStandard52.ApiSchema",
                "version": "1.0.335",
                "feedUrl": "https://pkgs.dev.azure.com/ed-fi-alliance/Ed-Fi-Alliance-OSS/_packaging/EdFi/nuget/v3/index.json"
            },
            {
                "name": "EdFi.DataStandard52.TPDM.ApiSchema",
                "version": "1.0.335",
                "feedUrl": "https://pkgs.dev.azure.com/ed-fi-alliance/Ed-Fi-Alliance-OSS/_packaging/EdFi/nuget/v3/index.json"
            }
        ]
    },
    "contracts": {
        "pluginsPackageVersion": "1.0.0",
        "customValidationPackageVersion": "1.0.0",
        "feedUrl": "https://pkgs.dev.azure.com/ed-fi-alliance/Ed-Fi-Alliance-OSS/_packaging/EdFi/nuget/v3/index.json"
    },
    "requires": [
        "DMS-1499 LoadPlugins startup phase",
        "DMS-1433 custom-validator fan-in pipeline step"
    ]
}
