[
  {
    "category": "Composition",
    "description": "Add an exclusive `UseRelationalBackend` repository swap in the host composition root. Acceptance: when `UseRelationalBackend=false`, DI still registers only the legacy `PostgresqlDocumentStoreRepository`; when `UseRelationalBackend=true`, DI registers only a new `RelationalDocumentStoreRepository` as `IDocumentStoreRepository`, adds the full relational runtime prerequisites for PostgreSQL deployments, and adds the matching full relational runtime prerequisites for MSSQL deployments, including runtime mapping-set compilation or provider services plus the dialect-specific relational command and reference-resolution services required by the new repository. No operation delegates back to the legacy repository when the flag is on.",
    "steps-to-verify": [
      "Add frontend/unit composition tests for PostgreSQL with the flag off, PostgreSQL with the flag on, and MSSQL with the flag on.",
      "Run: `dotnet test --no-restore ./src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj --filter WebApplicationBuilderExtensionsTests`"
    ],
    "completed": true
  },
  {
    "category": "Contracts",
    "description": "Define the backend-local DMS-983 write contracts actually supported in this branch: relational operation kind, target context, `FlatteningInput`, `FlattenedWriteSet`, root row buffer, root-extension rows, collection candidates, candidate-attached aligned-scope data, and `IRelationalWriteTerminalStage`. Acceptance: contracts use the selected `JsonNode` body rather than Core profile types, root `DocumentId` and new `CollectionItemId` remain unresolved for create flows, and collection-aligned 1:1 scopes such as `$.addresses[*]._ext.sample` remain attached to the owning collection candidate instead of being emitted as standalone bound rows.",
    "steps-to-verify": [
      "Add backend/unit contract tests that lock down the unresolved-key and candidate-attached-scope behavior.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Repository",
    "description": "Implement `RelationalDocumentStoreRepository` as the exclusive opt-in write repository. Acceptance: POST and PUT for non-descriptor resources enter the relational seam; GET and DELETE fail fast with `UnknownFailure` and precise not-implemented messages; descriptor POST and PUT also fail fast with precise not-implemented messages; missing non-descriptor `ResourceWritePlan` and unsupported relational gaps return deterministic `UnknownFailure` messages; no legacy fallback path is used anywhere.",
    "steps-to-verify": [
      "Add backend/unit tests for unsupported operations, descriptor writes, and missing-plan guard rails.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Write Orchestration",
    "description": "Add target-context selection ahead of flattening. Acceptance: POST performs real relational upsert existence detection using the request `ReferentialId` and passes `create-new` versus `existing` target context to the terminal stage, including existing `DocumentId` and `DocumentUuid` when found; PUT resolves the requested `DocumentUuid` to existing-document context before flattening and passes an update target context that includes the real existing `DocumentId` and `DocumentUuid` when found, while still deferring legacy ETag, immutable-identity, no-op, and not-found behavior in this story.",
    "steps-to-verify": [
      "Add backend/unit tests for POST create-new detection, POST existing-document detection, PUT existing-document context resolution, and the resulting PUT target-context shape.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Reference Resolution",
    "description": "Integrate `IReferenceResolver` into the relational repository flow. Acceptance: POST and PUT call the real resolver with the selected mapping set and request-extracted references, relational writes short-circuit on missing or incompatible references using the existing `UpsertFailureReference` and `UpdateFailureReference` semantics, and neither the flattener nor the terminal stage is called when reference resolution fails.",
    "steps-to-verify": [
      "Add backend/unit tests for successful resolution and short-circuit failure behavior on both document and descriptor failures.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Flattening",
    "description": "Adapt `ResolvedReferenceSet` into flattener-local hot-loop lookups for document and descriptor FKs. Acceptance: compiled document-reference bindings can resolve FK values by ordinal path without runtime JSONPath evaluation, nested collection references map to the correct owning candidate or row, and descriptor lookups use the resolved concrete-path data instead of re-reading source JSON.",
    "steps-to-verify": [
      "Add backend/unit tests for root, nested, and repeated reference lookups keyed by ordinal path.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Flattening",
    "description": "Implement the initial flattener walker for the root table and the DMS-983-supported extension scopes. Acceptance: traversal reads the selected body once, emits deterministic values in `TableWritePlan.ColumnBindings` order for the root table and present root-extension scopes, treats the selected body as authoritative input, performs strict scalar reading against compiled types, and does not invoke a general-purpose JSONPath engine in the hot loop.",
    "steps-to-verify": [
      "Add backend/unit fixture tests for root and non-collection row emission, including a supplied-body-is-authoritative case.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Flattening",
    "description": "Populate non-storage-assigned write bindings during flattening. Acceptance: `KeyUnificationWritePlan` producers populate canonical storage columns and synthetic presence flags where required, `WriteValueSource.Precomputed` values are filled for non-storage-assigned bindings except storage-assigned root and collection identities, and unified-alias columns are never materialized as writable outputs.",
    "steps-to-verify": [
      "Add backend/unit tests against the focused stable-key fixture to prove key-unification precomputed values and presence flags are emitted correctly.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Flattening",
    "description": "Implement collection and common-type candidate extraction. Acceptance: the flattener emits deterministic candidates for top-level and nested collection scopes with ordinal path, request sibling order, semantic-identity values in compiled order, scalar/FK/precomputed values, and attached aligned-scope data; duplicate submitted semantic identities under the same stable parent are detected in DMS-983 and fail deterministically before terminal handoff.",
    "steps-to-verify": [
      "Add backend/unit tests for top-level collections, nested collections, request sibling order preservation, and duplicate semantic-identity detection.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Extensions",
    "description": "Handle root-scope `_ext` flattening. Acceptance: root-level extension rows are emitted only when extension data exists, root-level extension child collections flatten into collection candidates with preserved request order, and empty or absent `_ext` sites do not create rows.",
    "steps-to-verify": [
      "Add backend/unit tests for root `_ext` row emission and root-level extension child collections.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Extensions",
    "description": "Handle `_ext` within collection and common-type scopes, including collection-aligned one-to-one scopes and aligned extension child collections. Acceptance: aligned one-to-one scopes stay attached to the owning base collection candidate until stable `CollectionItemId` binding exists, nested extension child collections preserve sibling order and semantic identity, and root-level versus collection-aligned attachment rules match `extensions.md`.",
    "steps-to-verify": [
      "Add backend/unit tests using the collections-with-nested-extension and focused stable-key extension fixtures.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Terminal Stage",
    "description": "Implement `IRelationalWriteTerminalStage` orchestration and a production default stage for DMS-983. Acceptance: after successful plan selection, target-context resolution, reference resolution, and flattening, the repository invokes the terminal stage with operation kind, target context, `ResourceWritePlan`, selected body, `ResolvedReferenceSet`, flattened output, and trace or diagnostic identifiers; the default production stage returns `UnknownFailure` with a precise not-implemented message; tests can substitute a capturing fake without changing repository logic.",
    "steps-to-verify": [
      "Add backend/unit tests for terminal-stage invocation, default not-implemented behavior, and capture-stage substitution.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Testing",
    "description": "Add Core-fronted seam integration coverage for the new relational write path using a capturing terminal stage. Acceptance: tests cover POST happy path, PUT happy path, reference-failure short-circuit, supplied-body-is-authoritative, mapping-set and write-plan guard rails, one complex fixture with nested collections plus `_ext`, and one top-level HTTP smoke with `UseRelationalBackend=true`; PostgreSQL and SQL Server composition are both covered at the registration and seam level, while heavier DB-backed write coverage remains deferred to DMS-984.",
    "steps-to-verify": [
      "Add Core-fronted integration tests in Core and one HTTP smoke in the frontend test project.",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Reference Resolution",
    "description": "Fail deterministically when a reference-bearing value is present in the selected write body but no matching `ResolvedReferenceSet` entry exists for the compiled binding and ordinal path. Acceptance: document-reference and descriptor-reference bindings distinguish `reference absent from body` from `reference present but missing from the resolved lookup set`; the missing-lookup case fails before terminal-stage handoff with a precise message that identifies the table or binding and path; the supported Core-fronted seam also surfaces that failure before any terminal-stage invocation; the flattener no longer emits `Literal(null)` for a present reference solely because extraction, lookup adaptation, or ordinal-path matching missed it.",
    "steps-to-verify": [
      "Add backend/unit tests where the selected body contains a root document reference, a nested collection document reference, and a descriptor value, but the `ResolvedReferenceSet` omits the corresponding concrete path. Assert that flattening or repository orchestration fails deterministically instead of producing a nullable FK binding.",
      "Add a Core seam test proving the supported relational write entrypoint fails before terminal-stage handoff when a present reference is missing from the resolved lookup set.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj --filter FullyQualifiedName~Relational_Write_Seam`"
    ],
    "completed": true
  },
  {
    "category": "Contracts",
    "description": "Narrow and simplify the DMS-983 standalone-scope contract so it matches the compiler shapes that can actually exist today. Acceptance: the branch no longer advertises generic base-resource non-collection row support when the current compiler cannot emit it; `StandaloneScopeWriteRowBuffer`, `NonCollectionRows`, related comments, and the DMS-983 design story are explicitly root-extension-scoped; stale shared-model comments that still imply broader support are corrected as part of the same cleanup; no dead or misleading contract surface remains that implies support for unsupported separate-table 1:1 base scopes.",
    "steps-to-verify": [
      "Update backend contract tests and flattener tests to lock down the final supported scope inventory and reject or omit impossible shapes consistently.",
      "Update shared model comments such as `DbTableKind.Collection` documentation so they no longer imply unsupported standalone-scope behavior in this branch.",
      "Update the DMS-983 story text so its contract language matches the runtime contract checked into code.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Boundary",
    "description": "Move `MappingSet` carriage back out of the public Core/backend repository request boundary and keep it backend-local. Acceptance: `EdFi.DataManagementService.Core.External` no longer references `EdFi.DataManagementService.Backend.External`; public `IUpsertRequest` and `IUpdateRequest` do not expose `MappingSet`; the relational path gets the resolved mapping set from an internal request type, internal adapter, or backend-local context instead of expanding the public request contract; legacy repository implementations and external consumers of `Core.External` do not need to take a backend-specific dependency to compile.",
    "steps-to-verify": [
      "Refactor the relational repository seam so only internal Core or backend-local request types carry the mapping-set dependency, then remove the `Core.External -> Backend.External` project reference.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Middleware",
    "description": "Clarify and align the missing-`MappingSet` contract across middleware, handlers, repository tests, and comments. Acceptance: missing `MappingSet` remains middleware-owned for supported execution paths and surfaces as a deterministic 503 before repository execution; middleware, handler, and repository-seam comments no longer claim null-tolerant downstream behavior if that is not actually supported; seam tests prove the supported relational path does not rely on an uncaught `ArgumentNullException`; the repository may keep null-argument checks as defensive invariants for unsupported direct-call scenarios, but no supported execution path ends in an uncaught `ArgumentNullException` because `MappingSet` was absent.",
    "steps-to-verify": [
      "Add or update unit tests and comments so the middleware-owned contract is explicit and the supported execution path does not rely on an uncaught null-argument exception.",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj --filter FullyQualifiedName~ResolveMappingSetMiddleware`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj --filter FullyQualifiedName~RelationalDocumentStoreRepositoryTests`"
    ],
    "completed": true
  },
  {
    "category": "Error Mapping",
    "description": "Replace the relational write seam's broad `InvalidOperationException`/`NotSupportedException` remap to `UnknownFailure` with typed request-validation or guard-rail classification for request-scoped flattening failures. Acceptance: duplicate submitted semantic identities, strict scalar-read failures caused by request payload shape/type mismatches, and other request-scoped flattener guard rails that represent client data or request-contract violations fail deterministically before terminal-stage handoff and surface through the relational/Core boundary as 400 `Data validation failed` responses instead of HTTP 500; `UnknownFailure` remains reserved for genuine unexpected server-side faults; descriptor-write and unsupported-operation guard rails that are intentionally not implemented remain explicit deterministic guard rails with their existing non-validation messaging.",
    "steps-to-verify": [
      "Introduce a typed relational write failure/result path that distinguishes request-validation failures from unexpected runtime failures, and thread it through `RelationalDocumentStoreRepository`, the Core write handlers, and any helper contracts needed to preserve current reference-failure behavior.",
      "Add backend and Core seam tests proving duplicate semantic identities under the same stable parent, present-but-request-invalid scalar values, and comparable request-scoped flattening guard rails fail before terminal-stage invocation and no longer become `UnknownFailure`/HTTP 500.",
      "Add handler or smoke-level tests proving the HTTP surface for these classified failures is 400 with the existing `Data validation failed. See 'validationErrors' for details.` shape, while unsupported relational operations and descriptor writes keep their explicit deterministic guard-rail messages.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj --filter FullyQualifiedName~Relational_Write_Seam`",
      "Run: `dotnet test --no-restore ./src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj --filter FullyQualifiedName~Given_A_Host_Using_The_Relational_Backend`"
    ],
    "completed": true
  },
  {
    "category": "Flattening",
    "description": "Tighten relational write scalar parsing so the flattener's type reads are strict for every compiled scalar kind, not just `Date`. Acceptance: `DateTime` and `Time` parsing use deterministic formats aligned to the compiled relational scalar contract instead of permissive culture-influenced parsing; invalid request values that survive upstream validation fail with the same typed request-validation path as other flattener scalar-read failures; existing valid fixture coverage for `Date`, `DateTime`, and `Time` continues to pass without relying on permissive parser behavior.",
    "steps-to-verify": [
      "Replace permissive `DateTime.TryParse`/`TimeOnly.TryParse` handling in `RelationalWriteFlattener` with exact format parsing that matches the supported API/body contract for compiled `ScalarKind.DateTime` and `ScalarKind.Time` values.",
      "Add backend/unit tests covering valid ISO-formatted `DateTime` and `Time` values, plus invalid but previously-permissive values that must now fail deterministically as scalar-read validation errors.",
      "Add or update seam tests proving strict scalar-read failures still short-circuit before terminal-stage invocation and participate in the typed 400 validation path instead of surfacing as HTTP 500.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj --filter FullyQualifiedName~RelationalWriteFlattenerTests`",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj --filter FullyQualifiedName~RelationalWriteSeam`"
    ],
    "completed": true
  },
  {
    "category": "Flattening",
    "description": "Refactor the DMS-983 flattener so it meets the story's single-traversal seam more literally and removes the duplicated root/body-index versus scope-local materialization paths. Acceptance: the selected request body is traversed through one structured walker that emits the root row, root-extension rows, collection candidates, and attached aligned scopes without a separate whole-body leaf index plus later collection re-walk; root-level and scope-local value materialization share one binding-evaluation path instead of maintaining duplicated `SelectedBodyIndex` and scope-node variants; the hot loop still avoids a general-purpose JSONPath engine and preserves the current deterministic request-order, ordinal-path, reference-lookup, and `_ext` behaviors.",
    "steps-to-verify": [
      "Collapse the duplicated root/scope materialization and key-unification evaluation paths in `RelationalWriteFlattener` into a single traversal-oriented implementation, removing dead or redundant helper paths introduced by the split design as part of the same refactor.",
      "Add backend/unit regression coverage that locks down current behavior for nested collections, root and collection-aligned `_ext`, semantic-identity extraction, reference binding by ordinal path, and the supplied-body-is-authoritative seam while exercising the refactored single-walker path.",
      "Add a targeted test or instrumentation assertion proving arrays are not skipped in an indexing pre-pass and then re-walked later as the primary execution model.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj --filter FullyQualifiedName~RelationalWriteFlattenerTests`",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj --filter FullyQualifiedName~RelationalWriteSeam`"
    ],
    "completed": true
  },
  {
    "category": "Boundary",
    "description": "Clarify in the relational write seam tests and helper comments that Core owns request normalization on the public HTTP path and the backend flattener only enforces strict parsing on the selected body it is handed. Acceptance: the handler-level strict datetime failure test in `RelationalWriteSeamTests` is renamed so it no longer implies the HTTP seam itself became stricter; the harness/setup comments make it explicit that these tests construct `RequestInfo` directly and bypass parse/coercion middleware; no nearby test names or comments describe backend-local strict parsing as a new externally observable POST/PUT contract change.",
    "steps-to-verify": [
      "Rename the strict datetime handler-seam test to say it covers an unnormalized supplied body or equivalent backend-local wording rather than the public HTTP seam.",
      "Add a short comment in the seam harness or request construction helper explaining that the test bypasses the normal Core parse and coercion middleware chain by injecting `ParsedBody` directly.",
      "Review the updated test names and comments to confirm they consistently say Core owns normalization and the backend consumes the already-selected request body.",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj --filter FullyQualifiedName~Given_Relational_Write_Seam`"
    ],
    "completed": true
  },
  {
    "category": "Testing",
    "description": "Add top-level relational HTTP smoke coverage that proves permissive date-time normalization still belongs to Core even when `UseRelationalBackend=true`. Acceptance: a frontend smoke test posts a payload containing a date-time value that Core currently normalizes, the request succeeds through the relational write seam, and the capturing flattener observes the normalized ISO-8601 value in `FlatteningInput.SelectedBody` rather than the original permissive input. The test must demonstrate the real middleware-backed HTTP path, not the handler harness that injects `ParsedBody` directly.",
    "steps-to-verify": [
      "Extend `RelationalWriteSmokeTests` with a POST case that includes a date-time field in the minimal schema plus a permissive input value that the existing Core coercion middleware normalizes.",
      "Assert from the capturing flattener that `FlatteningInput.SelectedBody` contains the normalized ISO-8601 value that Core produced before repository execution.",
      "Assert the HTTP response still indicates the relational write seam executed successfully so the test proves normalization and seam routing together.",
      "Run: `dotnet test --no-restore ./src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj --filter FullyQualifiedName~Given_A_Host_Using_The_Relational_Backend`"
    ],
    "completed": true
  },
  {
    "category": "Testing",
    "description": "Preserve explicit backend-local strict parsing coverage in direct flattener tests while making the boundary obvious. Acceptance: the direct `RelationalWriteFlattenerTests` continue to verify that non-ISO `DateTime` and non-ISO `Time` inputs are rejected when the backend is invoked with an unnormalized selected body, and the test names or comments clearly frame those failures as backend-local invariants rather than public HTTP behavior. Existing valid ISO parsing coverage remains in place.",
    "steps-to-verify": [
      "Review and update the direct flattener strictness test names or nearby comments so they explicitly describe backend-local strict parsing against the supplied selected body.",
      "Keep or add assertions that invalid non-ISO `DateTime` and `Time` values throw `RelationalWriteRequestValidationException` with the existing path-specific validation messages.",
      "Keep the valid ISO `DateTime` and `Time` parsing test coverage so the boundary is documented from both the success and failure directions.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj --filter FullyQualifiedName~RelationalWriteFlattenerTests`"
    ],
    "completed": false
  },
  {
    "category": "Documentation",
    "description": "Align branch-local review signals with the intended Core-versus-backend normalization boundary so future readers do not reopen the same false defect. Acceptance: any branch-local notes, comments, or test descriptions near the new strict parsing behavior explicitly state that Core normalization still governs the supported HTTP path and the stricter backend readers only validate the selected body presented to the flattener. The branch should no longer contain nearby wording that implies DMS-983 changed the external POST/PUT date-time contract.",
    "steps-to-verify": [
      "Update the most relevant comment or descriptive text near the relational seam strictness coverage so it states the intended boundary in one place future reviewers will see.",
      "Review the changed branch-local text together with the renamed tests to confirm it no longer reads like an externally visible strictness change.",
      "If any docs or committed progress notes in this branch reference the earlier review framing, update them to match the accepted boundary.",
      "Run: `git diff -- tasks.json src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/Handler/RelationalWriteSeamTests.cs src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/RelationalWriteFlattenerTests.cs src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/RelationalWriteSmokeTests.cs progress.txt`"
    ],
    "completed": false
  }
]
