[
  {
    "category": "Inventory",
    "description": "Capture the active legacy-backend dependency surface before making removals.",
    "acceptance-criteria": [
      "Inventory covers old PostgreSQL projects, runtime feature flags, E2E lane filters, database setup flags, installer references, route-context legacy schema assumptions, and legacy CDC references.",
      "Inventory searches include hidden docker-compose `.env*` files where legacy variables are known to live.",
      "Historical archive and design-note documents are identified as out of scope for cleanup.",
      "Removal targets are grouped by product/runtime code, tests, Docker/scripts, CI, active docs, and solution/project references."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"Old\\\\.Postgresql|UseRelationalBackend|USE_RELATIONAL_BACKEND|AppSettings__UseRelationalBackend|relational-backend|relational-ci-shard|NEED_DATABASE_SETUP|DeployDatabaseOnStartup\" src/dms eng eng/docker-compose/.env* .github build-dms.ps1 docs README.md GETTING_STARTED.md AGENTS.md`",
      "Run: `rg -n \"MappingSet is not null|MappingSet is null|new QueryRequest|new GetRequest|ITokenInfoEducationOrganizationLookup|GetService<IChangeQueryRepository>\" src/dms`",
      "Run: `rg --hidden -n \"InstanceManagement|routeContext|dms\\\\.document|Backend\\\\.Installer|Installer/EdFi\\\\.DataManagementService\\\\.Backend\\\\.Installer\\\\.dll|to_debezium\" src/dms/tests/EdFi.InstanceManagement.Tests.E2E eng/docker-compose eng/docker-compose/.env* build-dms.ps1`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Infrastructure",
    "description": "Move PostgreSQL data-source cache/provider infrastructure from `Old.Postgresql` into `Backend.Postgresql`.",
    "acceptance-criteria": [
      "`NpgsqlDataSourceCache` and `NpgsqlDataSourceProvider` live in `src/dms/backend/EdFi.DataManagementService.Backend.Postgresql`.",
      "Namespaces and project references are updated so active code no longer imports `EdFi.DataManagementService.Old.Postgresql` for these types.",
      "The moved types remain PostgreSQL-specific and are not moved into the common backend project."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"NpgsqlDataSource(Cache|Provider)\" src/dms/backend/EdFi.DataManagementService.Backend.Postgresql src/dms`",
      "Run: `rg -n \"using EdFi\\\\.DataManagementService\\\\.Old\\\\.Postgresql|Old\\\\.Postgresql\" src/dms/backend src/dms/frontend src/dms/tests` and confirm no active imports remain for the moved infrastructure.",
      "Run: `dotnet build src/dms/backend/EdFi.DataManagementService.Backend.Postgresql/EdFi.DataManagementService.Backend.Postgresql.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Infrastructure",
    "description": "Recreate `PostgresqlServiceExtensions.AddPostgresqlDatastore(IConfiguration)` in `Backend.Postgresql` with only relational-safe registrations.",
    "acceptance-criteria": [
      "`AddPostgresqlDatastore(IConfiguration)` has the same public method name and signature in the `Backend.Postgresql` project.",
      "The method registers mapping-pack option binding, `MappingSetCompiler`, `NoOpMappingPackStore`, PostgreSQL `RuntimeMappingSetCompiler` with `SqlDialect.Pgsql` and `PgsqlDialectRules`, `IMappingSetProvider`, `NpgsqlDataSourceCache`, and scoped `NpgsqlDataSourceProvider`.",
      "The method does not register legacy document repositories, legacy query handlers, old hydrators, SQL actions, or old startup/deploy services.",
      "No forwarding extension, namespace alias, or compatibility shim remains in `Old.Postgresql`."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"AddPostgresqlDatastore|RuntimeMappingSetCompiler|PgsqlDialectRules|RelationalDocumentStoreRepository|IQueryHandler\" src/dms/backend/EdFi.DataManagementService.Backend.Postgresql src/dms/frontend`",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Infrastructure",
    "description": "Remove the legacy authorization adapter and switch callers to relational token-info lookup registrations.",
    "acceptance-criteria": [
      "`AuthorizationRepositoryTokenInfoEducationOrganizationLookupAdapter` is deleted.",
      "PostgreSQL runtime wiring uses `AddPostgresqlRelationalTokenInfoEducationOrganizationLookup`.",
      "MSSQL runtime wiring uses `AddMssqlRelationalTokenInfoEducationOrganizationLookup`.",
      "No active code keeps the legacy adapter or old authorization repository as a token-info lookup fallback."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"AuthorizationRepositoryTokenInfoEducationOrganizationLookupAdapter|Add(Postgresql|Mssql)RelationalTokenInfoEducationOrganizationLookup|PostgresqlAuthorizationRepository\" src/dms`",
      "Run the frontend unit tests after wiring changes: `dotnet test src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Runtime Wiring",
    "description": "Make DMS frontend datastore composition always use relational runtime services for PostgreSQL and MSSQL.",
    "acceptance-criteria": [
      "`WebApplicationBuilderExtensions.cs` no longer imports `EdFi.DataManagementService.Old.Postgresql`.",
      "The frontend no longer reads `UseRelationalBackend` or branches on it.",
      "PostgreSQL selection calls the new `Backend.Postgresql.AddPostgresqlDatastore`, `AddPostgresqlReferenceResolver`, PostgreSQL fingerprint/resource-key readers, PostgreSQL relational token-info lookup, and registers relational mapping initialization.",
      "MSSQL selection calls `AddMssqlRelationalRuntimeServices`, MSSQL fingerprint/resource-key readers, MSSQL relational token-info lookup, and registers relational mapping initialization.",
      "`RelationalDocumentStoreRepository` is registered directly as both `IDocumentStoreRepository` and `IQueryHandler` for supported datastores."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"UseRelationalBackend|ConfigureQueryHandler|AddPostgresqlQueryHandler|EdFi\\\\.DataManagementService\\\\.Old\\\\.Postgresql\" src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore`",
      "Run: `rg -n \"RelationalDocumentStoreRepository|RelationalBackendMappingInitializer|AddPostgresqlReferenceResolver|AddMssqlRelationalRuntimeServices\" src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore`",
      "Run: `dotnet test src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Database Startup",
    "description": "Remove DMS runtime database setup-on-startup behavior and settings while leaving CMS deployment settings intact.",
    "acceptance-criteria": [
      "DMS no longer reads or documents `NEED_DATABASE_SETUP`, `DMS_DEPLOY_DATABASE_ON_STARTUP`, `AppSettings:DeployDatabaseOnStartup`, or `AppSettings__DeployDatabaseOnStartup`.",
      "DMS frontend app settings, validators, startup phases, and `InitializeDatabase` runtime wiring no longer reference DMS database deployment.",
      "CMS `DeployDatabaseOnStartup` settings and documentation remain unchanged.",
      "No replacement relational runtime startup provisioner is added."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|AppSettings__DeployDatabaseOnStartup|DeployDatabaseOnStartup|InitializeDatabase|IDatabaseDeploy\" src/dms eng eng/docker-compose/.env* .github build-dms.ps1 docs README.md GETTING_STARTED.md AGENTS.md`",
      "Inspect remaining `DeployDatabaseOnStartup` matches and confirm they are CMS-only or historical out-of-scope references.",
      "Run frontend unit tests: `dotnet test src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Database Startup",
    "description": "Delete DMS deployer contracts, PostgreSQL/MSSQL deploy implementations, and deployer unit tests.",
    "acceptance-criteria": [
      "DMS `IDatabaseDeploy`, `DatabaseDeployResult`, PostgreSQL `Deploy/DatabaseDeploy`, and MSSQL `Deploy/DatabaseDeploy` are deleted.",
      "Unit tests that only covered the deleted DMS deployer path are removed.",
      "Remaining DMS callers no longer reference deployer contracts or implementations.",
      "Manual MSSQL relational provisioning remains documented through `SchemaTools ddl provision --dialect mssql`, not a runtime installer.",
      "No new MSSQL Docker or E2E automation is added as part of removing the legacy startup provisioner."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"IDatabaseDeploy|DatabaseDeployResult|Deploy/DatabaseDeploy|SchemaTools ddl provision\" src/dms eng docs README.md GETTING_STARTED.md AGENTS.md`",
      "Run: `dotnet test src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj -c Release`",
      "Run: `dotnet test src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj -c Release` if PostgreSQL integration dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Installer Removal",
    "description": "Delete the DMS `Backend.Installer` project and remove it from solutions and project references.",
    "acceptance-criteria": [
      "`src/dms/backend/EdFi.DataManagementService.Backend.Installer` is deleted.",
      "`Backend.Installer` project entries are removed from `src/dms/EdFi.DataManagementService.sln` and `src/dms/EdFi.DataManagementService-Docker.sln` using `dotnet sln remove` where possible.",
      "No remaining DMS project references the installer project.",
      "No stale solution GUID or configuration blocks remain for the installer."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"Backend\\\\.Installer|EdFi.DataManagementService.Backend.Installer\" src/dms/EdFi.DataManagementService.sln src/dms/EdFi.DataManagementService-Docker.sln src/dms`",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Installer Removal",
    "description": "Remove legacy backend installer startup and Docker image plumbing.",
    "acceptance-criteria": [
      "`src/dms/run.sh` no longer invokes `/app/Installer/EdFi.DataManagementService.Backend.Installer.dll -e postgresql`.",
      "`src/dms/Dockerfile` no longer copies old backend source/projects or restores, publishes, or copies the backend installer.",
      "The built DMS Docker image does not contain `/app/Installer`.",
      "DMS container startup no longer logs or executes legacy schema installation."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"Installer/EdFi\\\\.DataManagementService\\\\.Backend\\\\.Installer\\\\.dll|/app/Installer|Backend\\\\.Installer|Old\\\\.Postgresql\" src/dms/run.sh src/dms/Dockerfile`",
      "Build the DMS Docker image and inspect that `/app/Installer` is absent."
    ],
    "completed": true
  },
  {
    "category": "Installer Removal",
    "description": "Remove backend installer publish targets from `build-dms.ps1`.",
    "acceptance-criteria": [
      "`build-dms.ps1` no longer defines `PublishBackendInstaller`.",
      "`build-dms.ps1` no longer invokes backend installer publish steps in any build lane.",
      "Build output preparation no longer creates or copies installer output directories.",
      "No old PostgreSQL backend project is published as part of DMS build-script output."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"PublishBackendInstaller|Backend\\\\.Installer|Old\\\\.Postgresql|/app/Installer|Installer/EdFi\\\\.DataManagementService\\\\.Backend\\\\.Installer\\\\.dll\" build-dms.ps1`",
      "Run the relevant build lane or inspect build output creation when local build dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Installer Removal",
    "description": "Remove backend installer entries from NuGet, package, and release artifact definitions.",
    "acceptance-criteria": [
      "The frontend nuspec no longer includes backend installer files or `/app/Installer` output.",
      "Release and package artifact scripts no longer upload, download, copy, or validate `Backend.Installer` output.",
      "CI or release checks no longer expect the old backend installer artifact to exist.",
      "Package output contains only active DMS runtime artifacts."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"Backend\\\\.Installer|/app/Installer|Installer/EdFi\\\\.DataManagementService\\\\.Backend\\\\.Installer\\\\.dll|Old\\\\.Postgresql\" src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore/EdFi.DataManagementService.Frontend.AspNetCore.nuspec .github eng`",
      "Build or inspect package artifacts when package dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "E2E Provisioning",
    "description": "Rename and refactor relational E2E database provisioning into the neutral `provision-e2e-database.ps1` helper.",
    "acceptance-criteria": [
      "`eng/docker-compose/provision-relational-e2e-database.ps1` is renamed to `eng/docker-compose/provision-e2e-database.ps1`.",
      "The renamed script owns explicit E2E reset/provision behavior and reads `E2E_DATABASE_NAME`.",
      "The script remains PostgreSQL-only and accepts explicit database names for Instance Management setup.",
      "`eng/docker-compose/provision-dms-schema.ps1` remains focused on CMS-selected target provisioning and is not used as the E2E reset/provision path.",
      "DMS E2E, build-script, workflow, template, unit-test, and documentation callers invoke the renamed script."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"provision-relational-e2e-database|provision-e2e-database|RELATIONAL_E2E_DATABASE_NAME|E2E_DATABASE_NAME\" eng eng/docker-compose/.env* src/dms .github build-dms.ps1 docs README.md GETTING_STARTED.md AGENTS.md`",
      "Run Pester tests under `eng/docker-compose/tests` that cover provisioning scripts.",
      "Run a DMS E2E setup path through `./build-dms.ps1 E2ETest -Configuration Release -SkipDockerBuild -IdentityProvider self-contained -EnvironmentFile './.env.e2e'` when local Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Docker Compose",
    "description": "Update docker-compose start scripts to remove legacy installer and database-setup assumptions.",
    "acceptance-criteria": [
      "`eng/docker-compose/start-local-dms.ps1` and `start-published-dms.ps1` no longer branch on legacy setup flags or invoke the deleted installer.",
      "`eng/docker-compose/start-all-services.ps1` no longer contains legacy connector or database setup branches tied to the deleted backend.",
      "Start scripts keep `eng/docker-compose/provision-dms-schema.ps1` focused on CMS-selected target provisioning and do not use it as the E2E reset/provision path.",
      "Start-script help, parameters, and examples do not mention removed DMS backend/setup variables."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|USE_RELATIONAL_BACKEND|Backend\\\\.Installer|/app/Installer|provision-dms-schema\" eng/docker-compose/start-local-dms.ps1 eng/docker-compose/start-published-dms.ps1 eng/docker-compose/start-all-services.ps1 eng/docker-compose/.env*`",
      "Start the local compose stack when Docker dependencies are available and confirm DMS starts without installer or legacy setup logs."
    ],
    "completed": true
  },
  {
    "category": "Docker Compose",
    "description": "Update docker-compose bootstrap modules and environment utility helpers for the single backend path.",
    "acceptance-criteria": [
      "`eng/docker-compose/bootstrap-*.psm1` no longer emits or depends on legacy backend/setup variables.",
      "`eng/docker-compose/env-utility.psm1` no longer reads, defaults, or writes legacy backend/setup variables.",
      "Hidden `.env*` files under `eng/docker-compose` no longer carry `USE_RELATIONAL_BACKEND`, `NEED_DATABASE_SETUP`, or DMS deploy-on-startup variables.",
      "Bootstrap and env utility behavior uses the neutral E2E provisioning helper where E2E database reset/provisioning is required."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|USE_RELATIONAL_BACKEND|AppSettings__UseRelationalBackend|AppSettings__DeployDatabaseOnStartup|provision-relational-e2e-database|provision-e2e-database\" eng/docker-compose/bootstrap-*.psm1 eng/docker-compose/env-utility.psm1 eng/docker-compose/.env*`",
      "Inspect remaining matches and confirm they are active neutral provisioning references only."
    ],
    "completed": true
  },
  {
    "category": "Docker Compose",
    "description": "Update docker-compose Pester tests for removed legacy installer and neutral provisioning behavior.",
    "acceptance-criteria": [
      "Pester tests under `eng/docker-compose/tests` are updated for the absence of backend installer plumbing.",
      "Pester tests cover the neutral `provision-e2e-database.ps1` helper where provisioning behavior is tested.",
      "Tests no longer assert or seed legacy backend/setup environment variables.",
      "Tests cover the expected start-script behavior after legacy setup branches are removed."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|USE_RELATIONAL_BACKEND|Backend\\\\.Installer|/app/Installer|provision-relational-e2e-database|provision-e2e-database\" eng/docker-compose/tests`",
      "Run the relevant Pester suite under `eng/docker-compose/tests`."
    ],
    "completed": true
  },
  {
    "category": "Instance Management E2E",
    "description": "Update Instance Management E2E setup script for relational route-context database provisioning.",
    "acceptance-criteria": [
      "`src/dms/tests/EdFi.InstanceManagement.Tests.E2E/setup-local-dms.ps1` starts DMS through the always-relational runtime path.",
      "The setup no longer calls the deleted installer or verifies, dumps, or replays `dms.document` schema state.",
      "Each route-context database is provisioned through the renamed explicit E2E reset/provision helper path.",
      "Verification checks use `dms.\"EffectiveSchema\"` and expected generated resource tables instead of `dms.document`.",
      "Existing route-context database-name constants or settings remain as-is and are passed explicitly to the provisioning helper; no CMS/config discovery is added.",
      "`.env.routeContext.e2e` remains neutral and does not depend on `USE_RELATIONAL_BACKEND`, `NEED_DATABASE_SETUP`, or `DMS_DEPLOY_DATABASE_ON_STARTUP`."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"Installer/EdFi\\\\.DataManagementService\\\\.Backend\\\\.Installer\\\\.dll|dms\\\\.document|Test-DmsDocumentTablePresent|USE_RELATIONAL_BACKEND|NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|EffectiveSchema|provision-e2e-database\" src/dms/tests/EdFi.InstanceManagement.Tests.E2E eng/docker-compose/.env.routeContext.e2e`",
      "Run Instance Management local setup when Docker dependencies are available and confirm each route-context database has `dms.\"EffectiveSchema\"`."
    ],
    "completed": true
  },
  {
    "category": "Instance Management E2E",
    "description": "Update `build-dms.ps1` InstanceE2ETest flow for relational route-context provisioning.",
    "acceptance-criteria": [
      "`build-dms.ps1 InstanceE2ETest` invokes the relational route-context provisioning flow before tests run.",
      "DMS is restarted after route-context database reprovisioning when needed to clear cached database state.",
      "The InstanceE2ETest flow no longer injects `AppSettings__UseRelationalBackend` or legacy DMS setup variables.",
      "Existing `instance-management-ci-shard-*` filters are preserved."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"InstanceE2ETest|routeContext|AppSettings__UseRelationalBackend|USE_RELATIONAL_BACKEND|NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|provision-e2e-database|EffectiveSchema\" build-dms.ps1 eng/docker-compose/.env.routeContext.e2e`",
      "Run: `./build-dms.ps1 InstanceE2ETest -Configuration Release -SkipDockerBuild` when Docker dependencies are available.",
      "Run existing `instance-management-ci-shard-*` filters during full validation."
    ],
    "completed": true
  },
  {
    "category": "Instance Management E2E",
    "description": "Update Instance Management E2E documentation for relational route-context setup.",
    "acceptance-criteria": [
      "`src/dms/tests/EdFi.InstanceManagement.Tests.E2E/README.md` describes relational route-context database setup.",
      "Documentation no longer references the deleted installer, `dms.document`, or legacy database setup flags.",
      "Documentation points to `provision-e2e-database.ps1` where explicit route-context provisioning is relevant.",
      "Shard-filter examples remain accurate for Instance Management E2E."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"Installer/EdFi\\\\.DataManagementService\\\\.Backend\\\\.Installer\\\\.dll|dms\\\\.document|USE_RELATIONAL_BACKEND|NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|provision-relational-e2e-database|provision-e2e-database|instance-management-ci-shard\" src/dms/tests/EdFi.InstanceManagement.Tests.E2E/README.md`",
      "Manually inspect remaining matches and confirm they describe the new relational route-context flow."
    ],
    "completed": true
  },
  {
    "category": "Instance Management E2E",
    "description": "Delete Instance Management Kafka and Debezium helpers tied to the legacy document store.",
    "acceptance-criteria": [
      "`KafkaTopicPerInstance.feature` is deleted.",
      "`InstanceKafkaMessageCollector.cs`, `InstanceKafkaTestConfiguration.cs`, `KafkaTopicHelper.cs`, and `InstanceKafkaStepDefinitions.cs` are deleted unless a shared non-CDC portion remains actively used.",
      "`DebeziumConnectorClient.cs` and `PostgresReplicationCleanup.cs` are deleted unless retained code is shared and no longer configures the legacy connector path.",
      "No Instance Management E2E helper still references `edfi.dms.document`, `dms.document`, `to_debezium`, or legacy publication names."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"KafkaTopicPerInstance|InstanceKafka|KafkaTopicHelper|DebeziumConnectorClient|PostgresReplicationCleanup|edfi\\\\.dms\\\\.document|dms\\\\.document|to_debezium|publication\" src/dms/tests/EdFi.InstanceManagement.Tests.E2E`",
      "Run Instance Management E2E when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Legacy CDC",
    "description": "Remove active legacy document-store Debezium connector configuration and registration.",
    "acceptance-criteria": [
      "`eng/docker-compose/postgresql_connector.json` and `eng/docker-compose/data_store_connector_template.json` are deleted if they only configure legacy document-store CDC.",
      "`eng/docker-compose/setup-connectors.ps1` no longer registers the legacy document-store connector by default.",
      "`start-local-dms.ps1`, `start-published-dms.ps1`, and `start-all-services.ps1` no longer include legacy connector setup branches or comments.",
      "No docker-compose connector setup path references `dms.document`, `to_debezium`, or `edfi.dms.document`."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"postgresql_connector|data_store_connector|setup-connectors|to_debezium|edfi\\\\.dms\\\\.document|dms\\\\.document\" eng/docker-compose`",
      "Start the local compose stack and confirm connector registration is not attempted by default when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Legacy CDC",
    "description": "Require explicit opt-in for Kafka and Kafka UI compose infrastructure.",
    "acceptance-criteria": [
      "`eng/docker-compose/start-local-dms.ps1` no longer includes `kafka.yml` in its default compose file set.",
      "`eng/docker-compose/start-published-dms.ps1` no longer includes `kafka.yml` in its default compose file set.",
      "`src/dms/tests/EdFi.DataManagementService.Tests.E2E/setup-local-dms.ps1` no longer passes `-EnableKafkaUI` by default.",
      "Kafka, Kafka UI, and connector setup only start when an explicit switch or parameter requests Kafka infrastructure.",
      "Pester or script-level checks cover the default no-Kafka path and the explicit Kafka opt-in path if Kafka infrastructure remains supported."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"kafka\\\\.yml|EnableKafkaUI|EnableKafka|setup-connectors|Kafka UI|Kafka\" eng/docker-compose/start-local-dms.ps1 eng/docker-compose/start-published-dms.ps1 eng/docker-compose/start-all-services.ps1 src/dms/tests/EdFi.DataManagementService.Tests.E2E/setup-local-dms.ps1 eng/docker-compose/tests`",
      "Start the local DMS compose stack without Kafka switches and confirm Kafka/Kafka UI containers are not started.",
      "Start the local DMS compose stack with the explicit Kafka switch, if retained, and confirm Kafka infrastructure starts."
    ],
    "completed": true
  },
  {
    "category": "Legacy CDC",
    "description": "Delete DMS E2E Kafka helpers tied to legacy document-store topics and payloads.",
    "acceptance-criteria": [
      "DMS `KafkaMessaging.feature` is deleted.",
      "`KafkaMessageCollector.cs` and `KafkaStepDefinitions.cs` are deleted unless a shared non-CDC portion remains actively used.",
      "No DMS E2E scenario asserts the legacy `edfi.dms.document` topic or legacy document payload shape.",
      "No hidden switch or compatibility shim remains for the legacy document-topic behavior."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"KafkaMessaging|KafkaMessageCollector|KafkaStepDefinitions|edfi\\\\.dms\\\\.document|to_debezium|publication\" src/dms/tests/EdFi.DataManagementService.Tests.E2E`",
      "Run DMS E2E when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Legacy CDC",
    "description": "Update active CDC/Kafka documentation and RestClient examples to say relational CDC support is pending.",
    "acceptance-criteria": [
      "Active docs and RestClient examples no longer describe `dms.document`, `to_debezium`, or `edfi.dms.document` as the active DMS streaming contract.",
      "Kafka/CDC sections are reduced to the minimal note specified in the plan: relational CDC/Kafka support is pending a separate implementation.",
      "Generic Kafka UI setup instructions are retained only where they are not tied to the legacy document-store CDC path.",
      "Historical archive and design-note documents are left untouched."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"dms\\\\.document|to_debezium|edfi\\\\.dms\\\\.document|Kafka|CDC|Debezium\" docs README.md GETTING_STARTED.md getting-started.http src/dms/tests/RestClient src/dms eng/docker-compose`",
      "Manually inspect remaining active matches and confirm they are either the pending-support note, generic non-legacy Kafka material, or out-of-scope historical content."
    ],
    "completed": true
  },
  {
    "category": "Feature Flag Removal",
    "description": "Delete the `UseRelationalBackend` setting from DMS settings classes, appsettings files, and test overrides.",
    "acceptance-criteria": [
      "`UseRelationalBackend` is removed from core, frontend, and DMS E2E `AppSettings` classes.",
      "DMS `appsettings*.json`, integration-test configuration, and unit-test overrides no longer set the flag.",
      "Tests that only assert the flag default or disabled-flag behavior are removed or rewritten for always-relational behavior.",
      "No active code reads `USE_RELATIONAL_BACKEND` or `AppSettings__UseRelationalBackend`."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"UseRelationalBackend|USE_RELATIONAL_BACKEND|AppSettings__UseRelationalBackend\" src/dms eng eng/docker-compose/.env* .github build-dms.ps1 docs README.md GETTING_STARTED.md AGENTS.md`",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`",
      "Run: `dotnet test src/dms/tests/EdFi.DataManagementService.Tests.Integration/EdFi.DataManagementService.Tests.Integration.csproj -c Release` when integration dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Core Pipeline",
    "description": "Make database fingerprint, resource-key, and mapping-set middlewares always follow the relational path.",
    "acceptance-criteria": [
      "`ValidateDatabaseFingerprintMiddleware` no longer branches on the removed backend flag.",
      "`ValidateResourceKeySeedMiddleware` no longer branches on the removed backend flag.",
      "`ResolveMappingSetMiddleware` always resolves mapping metadata required by the relational pipeline.",
      "Disabled-flag tests for these middlewares are removed or rewritten to assert always-relational behavior."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"UseRelationalBackend|Given_UseRelationalBackend_Is_False|ValidateDatabaseFingerprintMiddlewareFeatureFlagTests|ValidateResourceKeySeedMiddleware|ResolveMappingSetMiddleware|MappingSet is null|MappingSet is not null\" src/dms/core src/dms/tests src/dms/frontend`",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Core Pipeline",
    "description": "Make document-info extraction middleware require relational mapping metadata.",
    "acceptance-criteria": [
      "`ExtractDocumentInfoMiddleware` no longer branches on the removed backend flag.",
      "`ExtractDocumentInfoMiddleware` uses relational mapping metadata when extracting document information.",
      "Toggle-based tests for disabled relational backend behavior are removed or rewritten.",
      "Missing mapping metadata is treated as a pipeline/configuration defect with an actionable failure."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"ExtractDocumentInfoMiddleware|UseRelationalBackend|Given_UseRelationalBackend_Is_False|MappingSet is null|MappingSet is not null\" src/dms/core src/dms/tests src/dms/frontend`",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Core Pipeline",
    "description": "Make profile write and filtering middleware relational-only.",
    "acceptance-criteria": [
      "`ProfileWriteValidationMiddleware` no longer chooses legacy write validation behavior.",
      "`ProfileWritePipelineMiddleware` no longer chooses legacy write pipeline behavior.",
      "`ProfileFilteringMiddleware` keeps only behavior needed by relational projection/content-type flow and deletes legacy read-profile filtering.",
      "Profile middleware tests no longer use legacy or disabled-backend setup paths."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"ProfileWriteValidationMiddleware|ProfileWritePipelineMiddleware|ProfileFilteringMiddleware|UseRelationalBackend|legacy|Legacy|Given_UseRelationalBackend_Is_False\" src/dms/core src/dms/tests src/dms/frontend`",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Core Pipeline",
    "description": "Make request handlers and relational request contracts require resolved mapping sets instead of selecting legacy request shapes.",
    "acceptance-criteria": [
      "`QueryRequestHandler` creates relational query requests unconditionally and removes legacy `new QueryRequest(...)` paths.",
      "`GetByIdHandler` creates relational get requests unconditionally and removes legacy `new GetRequest(...)` paths.",
      "Write/delete request contracts and helper methods that require mapping metadata expose `MappingSet` as non-nullable.",
      "`RequestInfo.MappingSet` may remain nullable during progressive middleware resolution, but handlers fail fast with an actionable `InvalidOperationException` if a required mapping set is missing.",
      "Tests cover missing mapping-set failures as server pipeline/configuration defects."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"new QueryRequest|new GetRequest|MappingSet is null|MappingSet is not null|MappingSet\\\\?\" src/dms`",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`",
      "Run API-level integration tests when dependencies are available: `dotnet test src/dms/tests/EdFi.DataManagementService.Tests.Integration/EdFi.DataManagementService.Tests.Integration.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Authorization",
    "description": "Remove legacy token-info lookup fallback behavior and authorization middleware flag plumbing.",
    "acceptance-criteria": [
      "`ITokenInfoEducationOrganizationLookup` is deleted.",
      "`GetTokenInfoHandler` requires the relational token-info lookup registration and no longer falls back to legacy behavior.",
      "`ApiService` no longer passes `UseRelationalBackend` into `ResourceActionAuthorizationMiddleware`.",
      "`ResourceActionAuthorizationMiddleware` removes the backend-flag constructor parameter and keeps relational authorization behavior.",
      "Authorization tests no longer use legacy-mode helpers."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"ITokenInfoEducationOrganizationLookup|ResourceActionAuthorizationMiddleware|UseRelationalBackend|legacy-mode|Legacy\" src/dms/core src/dms/frontend src/dms/tests`",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`",
      "Run: `dotnet test src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Startup Validation",
    "description": "Make relational startup validation tasks unconditional and update related tests and messages.",
    "acceptance-criteria": [
      "`ValidateDatabaseFingerprintReaderRegistrationTask`, `ValidateResourceKeyRowReaderRegistrationTask`, and `ValidateStartupInstancesTask` no longer branch on a relational-backend flag.",
      "Tests for disabled relational backend startup validation are removed or rewritten for always-on validation.",
      "Error messages that mention `UseRelationalBackend is enabled` are replaced with direct missing-registration or configuration messages.",
      "Missing `IChangeQueryRepository` continues to represent explicit unsupported change-query behavior, not legacy backend selection."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"UseRelationalBackend is enabled|ValidateStartupInstancesTask|ValidateDatabaseFingerprintReaderRegistrationTask|ValidateResourceKeyRowReaderRegistrationTask|GetService<IChangeQueryRepository>\" src/dms`",
      "Run: `dotnet test src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj -c Release`",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Old Project Removal",
    "description": "Delete old PostgreSQL backend projects and remove all remaining references from projects and solutions.",
    "acceptance-criteria": [
      "`src/dms/backend/EdFi.DataManagementService.Old.Postgresql` is deleted.",
      "`src/dms/backend/EdFi.DataManagementService.Old.Postgresql.Tests.Integration` is deleted.",
      "Project references to old PostgreSQL projects are removed from backend, frontend, and test projects.",
      "Old-project and old-test-project `InternalsVisibleTo` entries are removed from remaining projects.",
      "Old project entries and configuration blocks are removed from `src/dms/EdFi.DataManagementService.sln` and `src/dms/EdFi.DataManagementService-Docker.sln`."
    ],
    "steps-to-verify": [
      "Run solution removals with `dotnet sln remove`; hand-edit only stale GUID/configuration blocks that remain.",
      "Run: `rg -n \"Old\\\\.Postgresql|EdFi\\\\.DataManagementService\\\\.Old\\\\.Postgresql|InternalsVisibleTo.*Old\" src/dms`",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`"
    ],
    "completed": true
  },
  {
    "category": "E2E Tags",
    "description": "Remove relational backend E2E lane tags and rename relational shard tags to neutral E2E shard tags.",
    "acceptance-criteria": [
      "`@relational-backend` is removed from all DMS E2E `.feature` files.",
      "`relational-backend` NUnit category filters are removed from scripts, workflows, guardrail tests, and active docs.",
      "`@relational-ci-shard-1` through `@relational-ci-shard-4` are renamed to `@e2e-ci-shard-1` through `@e2e-ci-shard-4` in source `.feature` files and active references.",
      "Generated `.feature.cs` artifacts are refreshed only through the repository's generation workflow if needed, not edited manually.",
      "`RelationshipsWithEdOrgsOnlyRelational.feature` and `RelationshipsWithPeopleRelational.feature` are renamed to neutral filenames."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"@relational-backend|relational-backend|@relational-ci-shard|relational-ci-shard|RelationshipsWith.*Relational\" src/dms/tests eng eng/docker-compose/.env* .github build-dms.ps1 docs README.md GETTING_STARTED.md AGENTS.md`",
      "Run Reqnroll or test generation if that is the repository-standard way to refresh generated feature artifacts.",
      "Run a shard command such as `./build-dms.ps1 E2ETest -Configuration Release -SkipDockerBuild -IdentityProvider self-contained -EnvironmentFile './.env.e2e' -TestFilter 'Category=@e2e-ci-shard-3'` when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Build Script",
    "description": "Remove backend-lane filter logic from `build-dms.ps1`.",
    "acceptance-criteria": [
      "`ConvertTo-Boolean` usage for `USE_RELATIONAL_BACKEND` is removed.",
      "`Test-FilterIncludesRelationalCategory`, `Test-FilterExcludesRelationalCategory`, and `Assert-E2ETestLaneMatchesFilter` are deleted.",
      "Filters such as `Category=@relational-backend`, `Category=relational-backend`, and their negative forms are removed instead of translated to another backend lane.",
      "Shard filters are preserved only as neutral `@e2e-ci-shard-N` filters."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"USE_RELATIONAL_BACKEND|Test-FilterIncludesRelationalCategory|Test-FilterExcludesRelationalCategory|Assert-E2ETestLaneMatchesFilter|Category[!]?=?@?relational-backend|relational-ci-shard|Category.*relational\" build-dms.ps1`",
      "Run: `pwsh -NoLogo -NoProfile -Command ./build-dms.ps1 E2ETest -Configuration Release -SkipDockerBuild -IdentityProvider self-contained -EnvironmentFile './.env.e2e' -TestFilter 'Category=@e2e-ci-shard-3'` when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Build Script",
    "description": "Make DMS E2E database provisioning neutral and unconditional in `build-dms.ps1`.",
    "acceptance-criteria": [
      "`Initialize-RelationalE2EDatabase` is renamed to `Initialize-E2EDatabase`.",
      "`Initialize-E2EDatabase` calls `eng/docker-compose/provision-e2e-database.ps1`.",
      "DMS E2E always resets/provisions the E2E relational database before tests.",
      "Provisioning uses `E2E_DATABASE_NAME` and no longer depends on `RELATIONAL_E2E_DATABASE_NAME`."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"Initialize-RelationalE2EDatabase|Initialize-E2EDatabase|provision-relational-e2e-database|provision-e2e-database|RELATIONAL_E2E_DATABASE_NAME|E2E_DATABASE_NAME\" build-dms.ps1 eng/docker-compose eng/docker-compose/.env*`",
      "Run: `pwsh -NoLogo -NoProfile -Command ./build-dms.ps1 E2ETest -Configuration Release -SkipDockerBuild -IdentityProvider self-contained -EnvironmentFile './.env.e2e'` when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Build Script",
    "description": "Neutralize DMS E2E test environment injection and result naming in `build-dms.ps1`.",
    "acceptance-criteria": [
      "The DMS E2E test process always receives `AppSettings__DataStoreDatabaseName`.",
      "The DMS E2E test process no longer receives `AppSettings__UseRelationalBackend`.",
      "Test-result suffixes change from `relational` and `relational-shard-N` to `e2e` and `e2e-shard-N`.",
      "Log and result names do not imply a separate relational backend lane."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"AppSettings__DataStoreDatabaseName|AppSettings__UseRelationalBackend|relational-shard|e2e-shard|Relational E2E|DMS E2E\" build-dms.ps1`",
      "Run: `pwsh -NoLogo -NoProfile -Command ./build-dms.ps1 E2ETest -Configuration Release -SkipDockerBuild -IdentityProvider self-contained -EnvironmentFile './.env.e2e' -TestFilter 'Category=@e2e-ci-shard-3'` when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "E2E Environment",
    "description": "Consolidate relational E2E environment files into neutral names.",
    "acceptance-criteria": [
      "Final `eng/docker-compose/.env.e2e` contains the current relational E2E content with legacy backend switches removed and names neutralized.",
      "`RELATIONAL_E2E_DATABASE_NAME` is renamed to `E2E_DATABASE_NAME` in the final `.env.e2e`.",
      "`eng/docker-compose/.env.e2e.relational` is deleted after references are updated.",
      "`eng/docker-compose/.env.smoke.relational` is renamed to `.env.smoke` and `.env.template.relational` is renamed to `.env.template`.",
      "Obsolete legacy neutral env-file content is replaced by the relational content rather than retained beside the new neutral files."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"RELATIONAL_E2E_DATABASE_NAME|E2E_DATABASE_NAME|USE_RELATIONAL_BACKEND|\\\\.env\\\\.e2e\\\\.relational|\\\\.env\\\\.smoke\\\\.relational|\\\\.env\\\\.template\\\\.relational\" eng eng/docker-compose/.env* src/dms/tests build-dms.ps1 .github docs README.md GETTING_STARTED.md AGENTS.md`",
      "Run DMS E2E setup through `./build-dms.ps1 E2ETest -Configuration Release -SkipDockerBuild -IdentityProvider self-contained -EnvironmentFile './.env.e2e'` when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "E2E Helpers",
    "description": "Update DMS E2E helper settings for the single relational backend path.",
    "acceptance-criteria": [
      "DMS E2E helpers remove `AppSettings.UseRelationalBackend`.",
      "DMS E2E helpers remove `LegacyDataStoreDatabaseName` and other legacy database-name assumptions.",
      "`SearchContainerSetup` always uses the renamed `provision-e2e-database.ps1` reset/provision behavior.",
      "Direct legacy reset paths are removed from DMS E2E helper code.",
      "No helper keeps a hidden backend-selection switch or compatibility path for the deleted legacy backend."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"AppSettings\\\\.UseRelationalBackend|UseRelationalBackend|LegacyDataStoreDatabaseName|SearchContainerSetup|provision-relational-e2e-database|provision-e2e-database|legacy reset|LegacyDataStore\" src/dms/tests/EdFi.DataManagementService.Tests.E2E eng/docker-compose/.env* build-dms.ps1`",
      "Run DMS E2E setup through `./build-dms.ps1 E2ETest -Configuration Release -SkipDockerBuild -IdentityProvider self-contained -EnvironmentFile './.env.e2e'` when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "CI",
    "description": "Update DMS CI workflow jobs for the single backend path.",
    "acceptance-criteria": [
      "The `Run Old PostgreSQL Integration Tests` job is removed from `.github/workflows/on-dms-pullrequest.yml`.",
      "DMS E2E workflows run without `relational-backend` filters and use neutral E2E shard filters where needed.",
      "Nightly, scheduled build, pre-image, and smoke workflows use the neutral `.env.e2e` or renamed smoke/template environment files.",
      "Workflow job names no longer imply an old PostgreSQL or separate relational backend lane."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"Run Old PostgreSQL Integration Tests|relational-backend|relational-ci-shard|Relational E2E|\\\\.env\\\\.e2e\\\\.relational|\\\\.env\\\\.smoke\\\\.relational|\\\\.env\\\\.template\\\\.relational\" .github/workflows/on-dms-pullrequest.yml .github/workflows/nightly-keycloak-e2e.yml .github/workflows/scheduled-build.yml .github/workflows/scheduled-pre-image-test.yml .github/workflows/scheduled-smoke-test.yml`",
      "Review the DMS E2E workflow commands and confirm only neutral E2E shard filters remain."
    ],
    "completed": true
  },
  {
    "category": "CI",
    "description": "Update template workflows to remove relational-backend inputs and branches.",
    "acceptance-criteria": [
      "Template workflows remove `use_relational_backend` inputs.",
      "Template workflows remove conditional branches that select legacy versus relational backend behavior.",
      "Populated and minimal PostgreSQL template workflows use the single backend path.",
      "Template workflow examples use neutral environment files and E2E filters."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"use_relational_backend|USE_RELATIONAL_BACKEND|AppSettings__UseRelationalBackend|relational-backend|relational-ci-shard|\\\\.env\\\\.template\\\\.relational\" .github/workflows/build-populated-template.yml .github/workflows/EdFi.Api.Populated.Template.PostgreSQL.yml .github/workflows/EdFi.Api.Minimal.Template.PostgreSQL.yml`",
      "Review template workflow inputs and generated commands for neutral backend behavior."
    ],
    "completed": true
  },
  {
    "category": "CI",
    "description": "Update CI log summaries and package/release artifact handling.",
    "acceptance-criteria": [
      "Log export paths and job summaries use neutral `DMS E2E` naming instead of `Relational E2E`.",
      "Package and release workflows no longer publish `Backend.Installer` artifacts.",
      "Package and release workflows no longer upload, download, or validate backend installer artifacts.",
      "CI artifact checks no longer expect `/app/Installer` in image or package output."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"Relational E2E|Backend\\\\.Installer|/app/Installer|Installer/EdFi\\\\.DataManagementService\\\\.Backend\\\\.Installer\\\\.dll|PublishBackendInstaller\" .github/workflows`",
      "Review package and release workflow artifacts for removed installer expectations."
    ],
    "completed": true
  },
  {
    "category": "Documentation",
    "description": "Update root and Docker setup docs for the single DMS backend path.",
    "acceptance-criteria": [
      "`AGENTS.md`, `README.md`, `GETTING_STARTED.md`, `docs/DOCKER.md`, and `eng/docker-compose/README.md` describe the single relational DMS backend path.",
      "Setup instructions no longer tell developers to choose between legacy and relational DMS backends or E2E lanes.",
      "Examples no longer use `.env.e2e.relational`, `USE_RELATIONAL_BACKEND`, `AppSettings__UseRelationalBackend`, relational backend category filters, DMS deploy-on-startup settings, `RELATIONAL_E2E_DATABASE_NAME`, or relational-named E2E env/script files.",
      "Docs include the neutral DMS E2E command and neutral shard filter examples using `.env.e2e` and `@e2e-ci-shard-N`.",
      "Docker setup docs no longer describe `/app/Installer` or legacy startup provisioning."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"\\\\.env\\\\.e2e\\\\.relational|USE_RELATIONAL_BACKEND|AppSettings__UseRelationalBackend|Category[!]?=?@?relational-backend|NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|AppSettings(:|__)DeployDatabaseOnStartup|RELATIONAL_E2E_DATABASE_NAME|provision-relational-e2e-database|\\\\.env\\\\.smoke\\\\.relational|\\\\.env\\\\.template\\\\.relational|/app/Installer|Backend\\\\.Installer\" AGENTS.md README.md GETTING_STARTED.md docs/DOCKER.md eng/docker-compose/README.md`",
      "Inspect remaining matches and confirm they are CMS-only or intentionally retained pending-support notes."
    ],
    "completed": true
  },
  {
    "category": "Documentation",
    "description": "Update relational, configuration, and multitenancy docs for neutral provisioning names.",
    "acceptance-criteria": [
      "`docs/MULTI-TENANCY-GETTING-STARTED.md`, `docs/RELATIONAL-BACKEND.md`, and `docs/CONFIGURATION.md` no longer describe a selectable legacy backend.",
      "Docs use `E2E_DATABASE_NAME`, `provision-e2e-database.ps1`, `.env.e2e`, `.env.smoke`, and `.env.template` where those examples are relevant.",
      "Docs describe the required `SchemaTools ddl provision` workflow for relational provisioning where appropriate.",
      "CMS `DeployDatabaseOnStartup` documentation remains intact where it applies only to Configuration Service."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"\\\\.env\\\\.e2e\\\\.relational|USE_RELATIONAL_BACKEND|AppSettings__UseRelationalBackend|Category[!]?=?@?relational-backend|NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|AppSettings(:|__)DeployDatabaseOnStartup|RELATIONAL_E2E_DATABASE_NAME|provision-relational-e2e-database|\\\\.env\\\\.smoke\\\\.relational|\\\\.env\\\\.template\\\\.relational|SchemaTools ddl provision\" docs/MULTI-TENANCY-GETTING-STARTED.md docs/RELATIONAL-BACKEND.md docs/CONFIGURATION.md`",
      "Inspect remaining `DeployDatabaseOnStartup` matches and confirm they are CMS-only."
    ],
    "completed": true
  },
  {
    "category": "Documentation",
    "description": "Verify active documentation cleanup and leave historical docs untouched.",
    "acceptance-criteria": [
      "Active docs no longer contain legacy backend lane instructions, DMS deploy-on-startup instructions, or old E2E environment names.",
      "Active docs that mention Kafka/CDC state that relational CDC/Kafka support is pending a separate implementation.",
      "Historical archive and design-note documents are not edited."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"\\\\.env\\\\.e2e\\\\.relational|USE_RELATIONAL_BACKEND|AppSettings__UseRelationalBackend|Category[!]?=?@?relational-backend|NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|AppSettings(:|__)DeployDatabaseOnStartup|RELATIONAL_E2E_DATABASE_NAME|provision-relational-e2e-database|\\\\.env\\\\.smoke\\\\.relational|\\\\.env\\\\.template\\\\.relational\" AGENTS.md README.md GETTING_STARTED.md docs eng/docker-compose/README.md`",
      "Inspect remaining matches and confirm they are CMS-only, historical out-of-scope, or intentionally retained pending-support notes."
    ],
    "completed": true
  },
  {
    "category": "Formatting",
    "description": "Format changed C# code after the legacy-backend removal edits are complete.",
    "acceptance-criteria": [
      "All changed C# files under `src/dms` are formatted with CSharpier.",
      "Formatting changes are limited to files touched by the cleanup or files CSharpier must update because of those edits.",
      "No unrelated generated artifacts are reformatted unless required by the build/test workflow."
    ],
    "steps-to-verify": [
      "Run: `dotnet csharpier format src/dms`",
      "Run: `git diff --check`"
    ],
    "completed": true
  },
  {
    "category": "Static Validation",
    "description": "Run final static searches to confirm active legacy backend, flag, installer, lane, and CDC references are gone.",
    "acceptance-criteria": [
      "Active DMS runtime code, tests, CI, Docker, scripts, and active docs have no old PostgreSQL project references.",
      "Active references to `UseRelationalBackend`, relational backend E2E lane categories, DMS deploy-on-startup flags, installer artifacts, legacy document schema, and legacy CDC topic names are removed.",
      "Static searches include hidden docker-compose `.env*` files.",
      "Remaining `DeployDatabaseOnStartup` matches are CMS-only or historical out-of-scope.",
      "Remaining change-query unsupported behavior is explicit and not a legacy-backend selector.",
      "Historical archive and design-note documents are not edited solely to satisfy searches."
    ],
    "steps-to-verify": [
      "Run: `rg --hidden -n \"Old\\\\.Postgresql|EdFi\\\\.DataManagementService\\\\.Old\\\\.Postgresql\" src/dms eng eng/docker-compose/.env* .github build-dms.ps1 docs README.md GETTING_STARTED.md AGENTS.md`",
      "Run: `rg --hidden -n \"UseRelationalBackend|USE_RELATIONAL_BACKEND|AppSettings__UseRelationalBackend|relational-backend|relational-ci-shard\" src/dms eng eng/docker-compose/.env* .github build-dms.ps1 docs README.md GETTING_STARTED.md AGENTS.md`",
      "Run: `rg --hidden -n \"NEED_DATABASE_SETUP|DMS_DEPLOY_DATABASE_ON_STARTUP|DeployDatabaseOnStartup|Backend\\\\.Installer|dms\\\\.document|edfi\\\\.dms\\\\.document|to_debezium\" src/dms eng eng/docker-compose/.env* .github build-dms.ps1 docs README.md GETTING_STARTED.md AGENTS.md getting-started.http`",
      "Run: `rg --hidden -n \"RELATIONAL_E2E_DATABASE_NAME|provision-relational-e2e-database|\\\\.env\\\\.e2e\\\\.relational|\\\\.env\\\\.smoke\\\\.relational|\\\\.env\\\\.template\\\\.relational\" src/dms eng eng/docker-compose/.env* .github build-dms.ps1 docs README.md GETTING_STARTED.md AGENTS.md`",
      "Run: `rg -n \"MappingSet is not null|MappingSet is null|new QueryRequest|new GetRequest|ITokenInfoEducationOrganizationLookup|GetService<IChangeQueryRepository>\" src/dms`",
      "Run: `rg --hidden -n \"Test-DmsDocumentTablePresent|Installer/EdFi\\\\.DataManagementService\\\\.Backend\\\\.Installer\\\\.dll|dms\\\\.document|to_debezium\" src/dms/tests/EdFi.InstanceManagement.Tests.E2E eng/docker-compose eng/docker-compose/.env* build-dms.ps1`"
    ],
    "completed": true
  },
  {
    "category": "Build Validation",
    "description": "Run solution build and focused unit/integration tests after the cleanup.",
    "acceptance-criteria": [
      "DMS solution builds in Release configuration.",
      "Core, backend, frontend, and DMS unit guardrail test projects pass.",
      "PostgreSQL backend integration tests pass where local PostgreSQL dependencies are available.",
      "MSSQL backend integration tests pass where local SQL Server dependencies are available.",
      "API-level DMS integration tests pass where required database dependencies are available."
    ],
    "steps-to-verify": [
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`",
      "Run: `dotnet test src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj -c Release`",
      "Run: `dotnet test src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj -c Release`",
      "Run: `dotnet test src/dms/tests/EdFi.DataManagementService.Tests.Unit/EdFi.DataManagementService.Tests.Unit.csproj -c Release`",
      "Run when dependencies are available: `dotnet test src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj -c Release`",
      "Run when dependencies are available: `dotnet test src/dms/backend/EdFi.DataManagementService.Backend.Mssql.Tests.Integration/EdFi.DataManagementService.Backend.Mssql.Tests.Integration.csproj -c Release --filter \"Category=MssqlIntegration\"`",
      "Run when dependencies are available: `dotnet test src/dms/tests/EdFi.DataManagementService.Tests.Integration/EdFi.DataManagementService.Tests.Integration.csproj -c Release`"
    ],
    "completed": true
  },
  {
    "category": "Instance Management E2E",
    "description": "Medium: Remove stale Kafka setup, CI, and documentation leftovers from E2E validation.",
    "acceptance-criteria": [
      "`src/dms/tests/EdFi.InstanceManagement.Tests.E2E/setup-local-dms.ps1` no longer passes `-EnableKafkaUI` or `-EnableKafka` to `start-local-dms.ps1` by default.",
      "The setup output, comments, and usage example no longer state or imply that Kafka UI is enabled for the default Instance Management E2E setup.",
      "DMS and Instance Management E2E CI jobs no longer add Kafka host entries or set `KAFKA_BOOTSTRAP_SERVERS` when no Kafka scenarios are run.",
      "DMS E2E documentation no longer tells users to configure Kafka hosts or services for the default suite.",
      "Kafka and Kafka UI remain available only through explicit docker-compose start-script opt-in switches.",
      "Script-level guardrail tests cover the Instance Management setup script so it cannot reintroduce default Kafka or Kafka UI startup."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"EnableKafkaUI|EnableKafka|Kafka UI\" src/dms/tests/EdFi.InstanceManagement.Tests.E2E/setup-local-dms.ps1 eng/docker-compose/tests` and confirm Instance Management setup has no default opt-in.",
      "Run: `rg -n \"KAFKA_BOOTSTRAP_SERVERS|kafka:127.0.0.1|kafka-1:127.0.0.1|kafka-2:127.0.0.1|kafka-3:127.0.0.1|run kafka tests\" .github/workflows src/dms/tests/EdFi.DataManagementService.Tests.E2E/README.md` and confirm only explicit Kafka opt-in coverage remains.",
      "Run the relevant Pester suite under `eng/docker-compose/tests` after adding or updating the guardrail.",
      "Run: `./build-dms.ps1 InstanceE2ETest -Configuration Release -SkipDockerBuild` when Docker dependencies are available."
    ],
    "completed": true
  },
  {
    "category": "Container Runtime",
    "description": "Medium: Gate the DMS container PostgreSQL readiness wait so MSSQL deployments can start.",
    "acceptance-criteria": [
      "`src/dms/run.sh` no longer parses `DATABASE_CONNECTION_STRING_ADMIN` as a PostgreSQL connection string unless the configured datastore is PostgreSQL.",
      "The startup script does not run `pg_isready` or wait for PostgreSQL when the configured datastore is MSSQL.",
      "PostgreSQL container startup still waits for PostgreSQL readiness when that guard remains useful.",
      "The startup script remains valid Bash and does not reintroduce installer-era database provisioning behavior."
    ],
    "steps-to-verify": [
      "Run: `bash -n src/dms/run.sh`.",
      "Run: `rg -n \"pg_isready|DATABASE_CONNECTION_STRING_ADMIN|AppSettings__Datastore\" src/dms/run.sh` and confirm the PostgreSQL readiness logic is gated to PostgreSQL datastore startup.",
      "Run an MSSQL-configured DMS container smoke test when Docker dependencies are available and confirm startup reaches the DMS process without waiting on PostgreSQL."
    ],
    "completed": true
  },
  {
    "category": "Core Pipeline",
    "description": "Low: Remove no-op profile middleware left after relational profile handling moved into the handlers and write pipeline.",
    "acceptance-criteria": [
      "`ProfileFilteringMiddleware` and `ProfileWriteValidationMiddleware` are deleted instead of retained as pass-through pipeline steps.",
      "`ApiService` no longer adds the deleted no-op profile middleware to read or write pipelines.",
      "`DmsCoreServiceExtensions` no longer registers the deleted no-op profile middleware.",
      "Unit tests that only assert the no-op middleware calls `next()` are deleted, and remaining pipeline-ordering tests assert the direct neighboring steps that still perform work."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"ProfileFilteringMiddleware|ProfileWriteValidationMiddleware\" src/dms/core src/dms/tests src/dms/frontend` and confirm only historical or intentionally removed references remain.",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`.",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`."
    ],
    "completed": true
  },
  {
    "category": "Core Pipeline",
    "description": "Low: Remove the unused legacy reference-extraction compatibility mode from the relational-only write path.",
    "acceptance-criteria": [
      "`ReferenceExtractionMode.LegacyCompatibility` and its legacy-only exception branches are deleted.",
      "`ReferenceExtractor` keeps only the relational write-validation behavior used by production callers.",
      "Unit tests that exist only to exercise legacy compatibility extraction are deleted or rewritten to cover relational validation behavior.",
      "No production or test code passes a legacy reference-extraction mode after the cleanup."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"ReferenceExtractionMode|LegacyCompatibility|CreateLegacyCompatibility\" src/dms/core src/dms/backend src/dms/tests` and confirm no legacy compatibility extraction mode remains.",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`.",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`."
    ],
    "completed": true
  },
  {
    "category": "Authorization",
    "description": "Low: Simplify authorization middleware now that all resource authorization uses the relational path.",
    "acceptance-criteria": [
      "`ResourceActionAuthorizationMiddleware` no longer has an `IsRelationalBackendAuthorizationRequest` helper that returns true for every supported method.",
      "Missing claim-set metadata and missing authorization strategies use the relational security-configuration responses directly.",
      "Dead legacy forbidden-response and staged-not-implemented branches that can no longer execute are removed.",
      "`ProvideAuthorizationFiltersMiddleware` always preserves raw relational strategy names with empty filters and no longer branches on nullable `MappingSet` to run the legacy filter-provider path.",
      "Authorization unit tests assert the always-relational missing-security-metadata, missing-strategy, and authorization-filter behavior without backend-mode helpers or legacy filter-provider fixtures."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"IsRelationalBackendAuthorizationRequest|CreateNoStrategiesForbiddenResponse|TryCreateStagedNotImplementedResponse|Given_A_Legacy|legacy-mode|UseRelationalBackend\" src/dms/core src/dms/tests` and confirm no dead backend-selection branch remains.",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`.",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`."
    ],
    "completed": true
  },
  {
    "category": "Core Backend",
    "description": "Low: Delete the unused SuccessDocumentStoreRepository fake backend.",
    "acceptance-criteria": [
      "`src/dms/core/EdFi.DataManagementService.Core/Backend/SuccessDocumentStoreRepository.cs` is deleted.",
      "No production or test code references `SuccessDocumentStoreRepository`.",
      "Tests that need repository fakes keep using focused test doubles such as `NotImplementedDocumentStoreRepository` subclasses."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"SuccessDocumentStoreRepository\" src/dms` and confirm no references remain.",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`.",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`."
    ],
    "completed": true
  },
  {
    "category": "Authorization",
    "description": "Medium: Finish removing or restoring the old authorization-handler contract. `ProvideAuthorizationFiltersMiddleware.cs:49` now creates empty-filter evaluators, but public request contracts still expose `IResourceAuthorizationHandler` at `IUpsertRequest.cs:64`, core still constructs it at `GetByIdHandler.cs:41` and `UpsertHandler.cs:70`, and relational code uses `AuthorizationStrategyEvaluators` plus `AuthorizationContext` at `RelationalDocumentStoreRepository.cs:122` instead.",
    "acceptance-criteria": [
      "Either the legacy `IResourceAuthorizationHandler` compatibility path is restored with populated filters, or the unused handler/factory/filter-provider/validator surface is removed from production contracts and registrations.",
      "Public request contracts no longer instruct backend implementations to depend on an empty-filter `IResourceAuthorizationHandler` path unless that path is fully functional.",
      "`ResourceAuthorizationHandler` no longer consumes empty filters created by `ProvideAuthorizationFiltersMiddleware` as a latent compatibility path.",
      "`DmsCoreServiceExtensions.cs` no longer registers legacy authorization validators or filter providers unless they are still used by an active, tested compatibility path.",
      "Unit tests are removed or updated so they assert the final supported authorization contract rather than a half-removed legacy path."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"IResourceAuthorizationHandler|ResourceAuthorizationHandler|IAuthorizationValidator|IAuthorizationFiltersProvider|AuthorizationFiltersProvider|AuthorizationStrategyEvaluator\" src/dms/core src/dms/backend` and confirm the remaining surface matches the chosen contract.",
      "Run: `rg -n \"ResourceAuthorizationHandler: new ResourceAuthorizationHandler|AuthorizationContext|AuthorizationStrategyEvaluators\" src/dms/core src/dms/backend/EdFi.DataManagementService.Backend/RelationalDocumentStoreRepository.cs` and confirm core/backend request flow is not carrying an unused handler.",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`.",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`."
    ],
    "completed": true
  },
  {
    "category": "Core Pipeline",
    "description": "Medium: Remove old document-security and authorization-pathway computation that relational write authorization does not consume. `RelationalDocumentStoreRepository.cs:141` says `DocumentSecurityElements` and `AuthorizationPathways` are not used for relational write authorization or persistence, but POST/PUT/DELETE pipelines still run legacy steps at `ApiService.cs:219` and `ApiService.cs:233`.",
    "acceptance-criteria": [
      "POST, PUT, and DELETE pipelines no longer run middleware that only populates legacy `DocumentSecurityElements` or `AuthorizationPathways` for relational write authorization.",
      "Request DTOs and handler calls no longer carry `DocumentSecurityElements` or `AuthorizationPathways` when the relational backend does not consume them.",
      "Any remaining document-security extraction is tied to an active relational requirement and has tests that prove that requirement.",
      "Legacy failure modes from unused document-security/pathway middleware are removed from relational write requests.",
      "Comments in `RelationalDocumentStoreRepository.cs` accurately describe any remaining security data passed through the write path."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"DocumentSecurityElements|AuthorizationPathways|ExtractDocumentSecurity|ProvideAuthorizationPathways|RelationalDocumentStoreRepository\" src/dms/core src/dms/backend` and confirm remaining usage is active and intentional.",
      "Inspect `src/dms/core/EdFi.DataManagementService.Core/ApiService.cs` POST/PUT/DELETE pipelines and confirm legacy document-security/pathway steps are gone or justified by active relational behavior.",
      "Run: `dotnet test src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj -c Release`.",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`."
    ],
    "completed": true
  },
  {
    "category": "Runtime Wiring",
    "description": "Low: Remove duplicate relationship authorization service registration. `AddReferenceResolver` already calls `AddRelationalRelationshipAuthorizationServices` at `ReferenceResolverServiceCollectionExtensions.cs:187`, and frontend datastore wiring calls it again at `WebApplicationBuilderExtensions.cs:231`.",
    "acceptance-criteria": [
      "Relationship authorization services are registered from one clear composition point for each datastore.",
      "The duplicate `AddRelationalRelationshipAuthorizationServices` call in frontend datastore wiring is removed if `AddReferenceResolver` remains the owner.",
      "No relationship authorization service registration is lost for PostgreSQL or MSSQL.",
      "Service-registration tests or focused inspection verify that required relationship authorization services remain available."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"AddRelationalRelationshipAuthorizationServices|AddReferenceResolver\" src/dms/backend src/dms/frontend` and confirm there is no redundant registration call.",
      "Run: `dotnet test src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj -c Release`.",
      "Run: `dotnet build src/dms/EdFi.DataManagementService.sln -c Release`."
    ],
    "completed": true
  },
  {
    "category": "Documentation",
    "description": "Low: Update Docker documentation for removed installer-era PostgreSQL variables. `docs/DOCKER.md:23` still lists `POSTGRES_ADMIN_*`, `POSTGRES_PORT`, and `POSTGRES_HOST`, but the installer path is gone and `src/dms/run.sh:14` only parses `DATABASE_CONNECTION_STRING_ADMIN` for PostgreSQL readiness.",
    "acceptance-criteria": [
      "`docs/DOCKER.md` no longer documents installer-era `POSTGRES_ADMIN_*`, `POSTGRES_PORT`, or `POSTGRES_HOST` variables as active DMS Docker configuration.",
      "Docker documentation describes the active PostgreSQL readiness/configuration inputs used by `src/dms/run.sh`.",
      "Documentation does not reintroduce `/app/Installer`, backend installer, or DMS deploy-on-startup setup instructions.",
      "Any remaining PostgreSQL admin variables in docs are clearly tied to active scripts that still consume them."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"POSTGRES_ADMIN_|POSTGRES_PORT|POSTGRES_HOST|DATABASE_CONNECTION_STRING_ADMIN|/app/Installer|Backend\\\\.Installer|DeployDatabaseOnStartup\" docs/DOCKER.md src/dms/run.sh` and confirm the docs match active runtime behavior.",
      "Run: `rg -n \"POSTGRES_ADMIN_|POSTGRES_PORT|POSTGRES_HOST\" docs README.md GETTING_STARTED.md eng/docker-compose/README.md` and confirm remaining references are active or removed."
    ],
    "completed": false
  },
  {
    "category": "Code Cleanup",
    "description": "Low: Remove stale comments and XML docs that reference deleted legacy/profile classes. Examples include `ProfileResponseFilter` at `ProfileExtensionCanonicalizer.cs:31`, `ProfileWriteValidationMiddleware.MergeNestedObjectStrippedFields` and `Legacy ExternalResponse callers` at `RelationalReadMaterializer.cs:135`, and `ProfileWriteValidationMiddleware.MergeNestedObjectStrippedFields` at `RelationalReadMaterializerTests.cs:730`.",
    "acceptance-criteria": [
      "Comments and XML docs no longer reference deleted `ProfileResponseFilter` or `ProfileWriteValidationMiddleware` types.",
      "Comments no longer describe `Legacy ExternalResponse` behavior that is not part of the relational-only backend.",
      "Updated comments describe the current relational/profile behavior without stale compatibility terminology.",
      "No production behavior changes are introduced by the documentation cleanup."
    ],
    "steps-to-verify": [
      "Run: `rg -n \"ProfileResponseFilter|ProfileWriteValidationMiddleware|Legacy ExternalResponse\" src/dms -g '*.cs'` and confirm stale references are gone or intentionally retained in historical test names only.",
      "Run: `dotnet csharpier format src/dms/core/EdFi.DataManagementService.Core/Profile/ProfileExtensionCanonicalizer.cs src/dms/backend/EdFi.DataManagementService.Backend/RelationalReadMaterializer.cs src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/RelationalReadMaterializerTests.cs`.",
      "Run: `git diff --check`."
    ],
    "completed": true
  }
]
