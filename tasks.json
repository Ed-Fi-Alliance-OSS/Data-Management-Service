[
  {
    "category": "Reference Field Grouping",
    "description": "Add one authoritative helper or metadata shape that derives logical reference-field groups from `DocumentReferenceBinding.IdentityBindings` by grouping repeated `ReferenceJsonPath` values within a single `DocumentReferenceBinding`. Acceptance: grouping is limited to one reference site (`ReferenceObjectPath` + owning table), preserves first-seen field order, exposes all physical member columns for each logical field, and does not legitimize the existing arbitrary duplicate-source-path case where the same JSON path is duplicated outside one reference binding.",
    "steps-to-verify": [
      "Add unit tests that build a `DocumentReferenceBinding` with repeated `ReferenceJsonPath` values and assert the grouped output preserves first-seen logical field order while keeping all physical member columns for the duplicated field.",
      "Add a negative unit test proving the grouping helper does not merge bindings from different `ReferenceObjectPath` values or unrelated duplicate source-path columns.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit.csproj --filter FullyQualifiedName~KeyUnification`"
    ],
    "completed": true
  },
  {
    "category": "Key Unification Endpoint Resolution",
    "description": "Update `KeyUnificationPass` so explicit equality-constraint endpoint resolution accepts one same-site grouped reference field when repeated bindings come from a single flattened reference site, instead of failing any time multiple physical bindings exist. Acceptance: when a grouped reference field participates in a non-redundant equality constraint, key unification adds edges for every physical member selected by that grouped endpoint; when both endpoints resolve to the same same-site logical endpoint group, the constraint remains redundant and intra-group unification comes from the implicit same-site edge rule; existing cross-table and unsupported-kind behavior is unchanged; the current arbitrary ambiguous-endpoint scenario still fails fast with the existing diagnostic.",
    "steps-to-verify": [
      "Add a TPDM-like minimal relational-model test where one equality-constraint endpoint resolves to two physical columns from one flattened reference site, the opposite endpoint resolves to a different binding or grouped endpoint on the same table, and assert key unification succeeds.",
      "Retain the existing ambiguous-endpoint negative test and assert it still throws `resolved to multiple distinct bindings` for an arbitrary duplicated scalar path.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit.csproj --filter FullyQualifiedName~Given_Key_Unification`"
    ],
    "completed": true
  },
  {
    "category": "Implicit Same-Site Unification",
    "description": "Teach key unification to synthesize implicit edges for duplicate logical reference fields inside one flattened reference site even when no explicit `equalityConstraints` mention those fields. Acceptance: repeated `ReferenceJsonPath` members within one grouped reference field are unified to one canonical storage class without TPDM-specific branching; deterministic ordering of classes and diagnostics is preserved; invalid mixed-kind or descriptor-target-mismatch groups still fail with actionable errors.",
    "steps-to-verify": [
      "Add a minimal schema test with duplicate reference-field bindings but no explicit equality constraint for one of the duplicated fields, and assert the field still converges to one key-unification class.",
      "Add a negative test proving a duplicated logical field that mixes scalar and descriptor members, or mismatched descriptor targets, still fails deterministically.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit.csproj --filter \"FullyQualifiedName~Given_Key_Unification|FullyQualifiedName~ConstraintDerivation\"`"
    ],
    "completed": true
  },
  {
    "category": "Abstract Identity Root Resolution",
    "description": "Relax abstract-identity root-column lookup so duplicate root `SourceJsonPath` values are allowed only when same-kind columns converge to one stored column after key unification. Acceptance: abstract identity derivation succeeds for unified flattened-reference duplicates, still fails for arbitrary duplicate root columns that do not converge to one stored column, and does not weaken the existing fail-fast path for conflicting kinds.",
    "steps-to-verify": [
      "Split the current duplicate-root negative coverage into two tests: one that still fails for artificial non-unified duplicates and one that succeeds for a key-unified flattened-reference case.",
      "Add or update tests that assert conflicting column kinds on the same root `SourceJsonPath` still fail with an actionable diagnostic.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit.csproj --filter \"FullyQualifiedName~Abstract_Identity|FullyQualifiedName~Key_Unification\"`"
    ],
    "completed": true
  },
  {
    "category": "Reference Projection Compiler And Validator Switch",
    "description": "Update `ReferenceIdentityProjectionPlanCompiler` and `ReadPlanProjectionContractValidator` together so compile-time read-plan generation works on grouped logical reference fields instead of raw `DocumentReferenceBinding.IdentityBindings` count and index. Acceptance: compiled projection metadata contains unique logical field paths per reference object, preserves deterministic field ordering, still uses the stored `DocumentFk` column for `FkColumnOrdinal`, and valid grouped projections pass compile-time validation while duplicated, missing, or reordered logical reference fields fail with deterministic messages that name the offending `ReferenceObjectPath` and `ReferenceJsonPath`.",
    "steps-to-verify": [
      "Add a read-plan compiler test for a flattened reference binding with duplicate `ReferenceJsonPath` values and assert the compiled projection contains one entry for the logical field rather than duplicate field ordinals.",
      "Add validator negative tests for duplicated logical reference fields, omitted logical reference fields, and reordered logical reference fields within one projection binding.",
      "Retain or add a positive regression test proving a normal reference projection without duplicate logical fields still compiles to the same ordinals as before.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj --filter FullyQualifiedName~Given_ReadPlanCompiler`"
    ],
    "completed": true
  },
  {
    "category": "Normalized Grouped Projection Decode",
    "description": "Update the normalized read-plan decode path so grouped logical reference-field validation matches the compile-time rule after the compiler and validator switch. Acceptance: normalized-plan decode uses the same grouped logical reference-field contract as compile-time validation; valid grouped projections round-trip unchanged; malformed grouped projections fail through decode-time validation with deterministic messages instead of relying on raw `IdentityBindings` count or index assumptions.",
    "steps-to-verify": [
      "Add or update normalized-plan decode coverage proving duplicated, omitted, or reordered grouped logical reference fields fail through decode-time validation with the same deterministic rule used at compile time.",
      "Add or update a positive normalized round-trip test proving a valid grouped projection survives encode/decode without changing logical field order or ordinals.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj --filter \"FullyQualifiedName~Given_ReadPlanCompiler|FullyQualifiedName~Given_NormalizedPlanContractCodec\"`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Startup Regression",
    "description": "Add startup regression coverage proving PostgreSQL runtime mapping initialization no longer dies on a flattened-reference schema that previously triggered ambiguous endpoint failure. Acceptance: a minimal TPDM-like schema with duplicate `ReferenceJsonPath` groups compiles through `PostgresqlRuntimeMappingSetCompiler` and allows startup cache priming to complete; a deliberately arbitrary duplicate-source-path schema still fails so startup does not silently accept unsupported ambiguity.",
    "steps-to-verify": [
      "Add a startup unit test that wires a minimal flattened-reference schema through `DmsStartupOrchestrator` or `PostgresqlRuntimeMappingSetCompiler` and asserts runtime mapping cache initialization succeeds.",
      "Add a negative startup or compiler-path test proving an arbitrary duplicated scalar path still fails during runtime mapping compilation.",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj --filter FullyQualifiedName~PostgresqlRuntimeMappingInitialization`"
    ],
    "completed": true
  },
  {
    "category": "CI Readiness Failure Log Surfacing",
    "description": "Update `.github/workflows/on-dms-pullrequest.yml` so the `build-and-start-dms` job exports Docker logs on failure and writes the `dms-local-dms` container log directly into the workflow output when readiness polling or OpenAPI download fails. Acceptance: a failed readiness timeout produces a `logs/` artifact for this job, includes inline DMS container logs in the failing job output, and leaves successful runs unchanged because the new log steps are guarded by `if: failure()`.",
    "steps-to-verify": [
      "Force `build-and-start-dms` to fail after `Start DMS Docker` and confirm the job uploads a logs artifact that contains the DMS and companion container logs from the local stack.",
      "Confirm the failing GitHub Actions log shows the `dms-local-dms` log inline without requiring artifact download to see the startup exception.",
      "Run a successful `build-and-start-dms` path and confirm the failure-only log export steps are skipped."
    ],
    "completed": true
  },
  {
    "category": "Schema Compile Preflight In CI",
    "description": "Add a preflight CI step that runs `dms-schema ddl emit` against the same schema set packaged into DMS startup before Docker is launched. Acceptance: schema-to-relational compile failures fail the workflow before `Start DMS Docker`, the step does not require database connectivity, and the workflow logs clearly identify the schema inputs and dialect being compiled.",
    "steps-to-verify": [
      "Run the new preflight step locally or in CI with the normal schema inputs and confirm it completes successfully without starting Docker or opening a database connection.",
      "Introduce or replay an extension-resource compile failure and confirm the workflow fails in the preflight `dms-schema ddl emit` step before the readiness loop is reached.",
      "Confirm the generated output is deterministic enough for CI use by rerunning the command with the same inputs and verifying it does not produce inconsistent manifests or SQL."
    ],
    "completed": false
  },
  {
    "category": "Extension Schema Startup Coverage",
    "description": "Extend PostgreSQL startup mapping tests to exercise real extension-bearing schemas instead of only the current minimal core-only fixture. Acceptance: `PostgresqlRuntimeMappingInitializationTests` covers at least one startup-success path and one startup-failure path using schema inputs that include extension resources, and failures surface the compile-time exception details rather than collapsing into a generic readiness symptom.",
    "steps-to-verify": [
      "Add a positive startup initialization test that loads a core schema plus at least one extension schema and asserts `DmsStartupOrchestrator` completes backend mapping initialization.",
      "Add a negative startup initialization test with an intentionally invalid extension-bearing schema and assert the thrown exception includes the relevant resource or invariant failure detail.",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj --filter FullyQualifiedName~PostgresqlRuntimeMappingInitialization`"
    ],
    "completed": false
  },
  {
    "category": "Relational Fixture Contract Alignment",
    "description": "Align relational-model fixtures and goldens with the production resource-key contract by excluding `isResourceExtension=true` resources from synthesized resource keys. Acceptance: `EffectiveSchemaSetFixtureBuilder` no longer invents keys for extension resources, authoritative expected manifests are regenerated accordingly, and Sample extension entries such as `Sample:Contact`, `Sample:School`, `Sample:Staff`, and `Sample:Student` no longer appear in `authoritative-effective-schema.manifest.json` resource keys unless the production contract changes first.",
    "steps-to-verify": [
      "Add or update unit coverage proving fixture-generated resource key entries skip extension resources the same way `ResourceKeySeedProvider` does.",
      "Regenerate the affected authoritative manifests and confirm extension resources are removed from the expected resource-key list while non-extension resources remain stable.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit/EdFi.DataManagementService.Backend.RelationalModel.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Startup Failure Status Surfacing",
    "description": "Design and, if justified, implement a startup-status surfacing mechanism so pre-bind startup failures are externally diagnosable without relying only on connection-refused symptoms. Acceptance: the chosen approach is documented and preserves fatal startup semantics while surfacing the failing startup phase and error summary through a stable signal usable by CI or local troubleshooting, such as a status artifact, structured log contract, or lightweight startup-state endpoint.",
    "steps-to-verify": [
      "Document the selected approach, rejected alternatives, and interaction with the current `Environment.Exit(-1)` startup failure path.",
      "Validate the design with a failing startup scenario and confirm CI or local tooling can report the startup phase and summarized failure reason without manually inspecting every container log.",
      "If code is added, run the relevant DMS startup and health/readiness test coverage or add new coverage that proves the status signal is emitted when startup fails before route binding."
    ],
    "completed": false
  }
]
