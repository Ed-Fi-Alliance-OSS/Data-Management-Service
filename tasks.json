[
  {
    "category": "Contract",
    "description": "Replace the resource-name-only document-reference failure surface in `src/dms/core/EdFi.DataManagementService.Core.External/Backend/UpsertResult.cs` and `src/dms/core/EdFi.DataManagementService.Core.External/Backend/UpdateResult.cs` with a path-aware per-occurrence contract for DMS-982. Acceptance: the new contract can represent every failing document-reference instance with its concrete JSON path, target resource, and an internal reason enum at minimum covering `Missing` and `IncompatibleTargetType`; the contract is usable by both POST and PUT handlers; and descriptor failure contracts remain available for existing 400-style mapping.",
    "steps-to-verify": [
      "Add or update unit tests that construct the new result types and confirm every failing occurrence can be represented without collapsing to unique resource names.",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Core Error Mapping",
    "description": "Update `src/dms/core/EdFi.DataManagementService.Core/Handler/UpsertHandler.cs`, `src/dms/core/EdFi.DataManagementService.Core/Handler/UpdateByIdHandler.cs`, and any shared response helpers so path-aware document-reference failures become 409 responses keyed by concrete JSON path instances. Acceptance: existing status semantics stay unchanged, every failing path instance is emitted in `validationErrors`, duplicate referential ids at different JSON locations produce multiple entries, and descriptor failures continue to map to 400 validation responses.",
    "steps-to-verify": [
      "Add handler tests covering POST and PUT document-reference failures with multiple concrete paths for the same missing referential id.",
      "Run: `dotnet test --no-restore ./src/dms/core/EdFi.DataManagementService.Core.Tests.Unit/EdFi.DataManagementService.Core.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Resolver Service",
    "description": "Add the reusable relational write-prerequisite resolver service and contracts for DMS-982 without wiring a new relational `IDocumentStoreRepository` path. Acceptance: the service accepts request-scoped extracted references plus `MappingSet`/resource context, performs per-request memoization and dedupe, preserves per-occurrence diagnostics in a `ResolvedReferenceSet`, and exposes a narrow adapter seam so future auth/write prerequisite batching can append SQL without redesigning the service contract.",
    "steps-to-verify": [
      "Add unit tests for request-scoped dedupe and memoization behavior using fake adapter results.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Relational Access Seam",
    "description": "Introduce a request-scoped relational command/connection seam usable by the DMS-982 resolver without wiring a new relational `IDocumentStoreRepository` path. Acceptance: PostgreSQL can execute resolver commands against the current per-request data source, SQL Server can execute resolver commands from the active request-scoped connection string path, shared resolver/adapter code depends on this seam instead of old repository operations, and the seam is narrow enough to support future batched write-prerequisite execution.",
    "steps-to-verify": [
      "Add unit tests covering the shared relational access abstraction and dialect-specific factory behavior used by the resolver.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Reference Path Indexing",
    "description": "Implement concrete reference-path indexing helpers that map Core-emitted `DocumentReference.Path` values to compiled `DocumentReferenceBinding` entries and request-local `ordinalPath` tuples for later flattening work. Acceptance: helpers resolve root, collection, and nested-collection reference-object paths against a compiled write plan; reject unknown, mismatched, or wildcard paths deterministically; and produce a per-request `(binding, ordinalPath)` entry for each extracted reference occurrence without introducing profile scope-address derivation responsibilities.",
    "steps-to-verify": [
      "Add unit tests for root, single-collection, and nested-collection reference paths, including invalid-path failures.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Compatibility Metadata",
    "description": "Implement metadata helpers that derive allowed target `ResourceKeyId` sets for document-reference validation from the runtime mapping set and derived relational model. Acceptance: concrete targets resolve to exactly one allowed `ResourceKeyId`, abstract/superclass targets resolve to the allowed concrete member keys for the hierarchy, descriptor resources are excluded from document-reference compatibility checks, and missing mapping-set metadata fails fast with deterministic errors.",
    "steps-to-verify": [
      "Add unit tests covering concrete targets, abstract targets, and missing-resource failures using compiled mapping fixtures.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "PostgreSQL Lookup SQL",
    "description": "Implement the PostgreSQL bulk lookup SQL shape and parameterization for DMS-982. Acceptance: document and descriptor referential ids can be emitted into a set-based PostgreSQL lookup against `dms.ReferentialIdentity`, no per-reference query loop is introduced, the SQL shape can later participate in a larger batched write-prerequisite command, and focused unit tests cover empty, small, and large deduped lookup sets plus deterministic parameter ordering.",
    "steps-to-verify": [
      "Add focused unit tests for PostgreSQL SQL shape generation and parameter binding across representative lookup sizes.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "PostgreSQL Adapter Wiring",
    "description": "Implement PostgreSQL adapter execution and raw row projection over the shared resolver contract. Acceptance: the adapter executes the PostgreSQL lookup through the shared relational access seam, projects rows from `dms.ReferentialIdentity`, `dms.Document`, and `dms.Descriptor` into shared raw result records, returns enough membership/type data for later shared failure classification, and preserves deduped lookup semantics while retaining per-occurrence inputs for diagnostics.",
    "steps-to-verify": [
      "Add focused unit tests for PostgreSQL raw result projection covering found, missing, alias-row, and descriptor-membership row shapes.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "SQL Server Small-List Strategy",
    "description": "Implement the SQL Server small-list lookup strategy for DMS-982 using a simple parameterized approach beneath safe thresholds. Acceptance: small deduped lookup sets execute without risking the 2,100-parameter limit, document and descriptor lookups share one resolver-oriented raw row contract, no per-reference query loop is introduced, and focused unit tests cover deterministic parameterization and row projection for representative small inputs.",
    "steps-to-verify": [
      "Add focused unit tests for the SQL Server small-list strategy and raw row projection contract.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "SQL Server Bulk Strategy",
    "description": "Implement the SQL Server threshold-safe bulk lookup strategy for large DMS-982 reference sets. Acceptance: large deduped lookup sets switch to a bulk-safe strategy before risking the 2,100-parameter limit, the strategy returns the same raw adapter row contract as the small-list path, and focused unit tests cover threshold crossing and deterministic batching behavior.",
    "steps-to-verify": [
      "Add focused unit tests that exercise the SQL Server threshold-crossing path and bulk-safe lookup mechanics.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "SQL Server Adapter Parity",
    "description": "Implement SQL Server adapter orchestration that selects between the small-list and bulk strategies and exposes the shared raw resolver result contract. Acceptance: strategy selection is deterministic, the adapter returns the membership/type data required for later shared failure classification, and focused unit tests assert raw-result parity with PostgreSQL across found, missing, incompatible-target, and descriptor-membership row shapes.",
    "steps-to-verify": [
      "Add focused unit tests for SQL Server strategy selection and raw-result parity with PostgreSQL.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Failure Classification",
    "description": "Implement shared failure classification over adapter results for both document and descriptor references. Acceptance: unresolved document references and wrong-type document matches surface through the new path-aware document-reference contract with internal reasons kept distinct for tests/logging, descriptor failures distinguish at least `Missing` versus `DescriptorTypeMismatch`, and every failing concrete JSON path instance is preserved even when the underlying lookup key was deduped.",
    "steps-to-verify": [
      "Add unit tests that exercise mixed document/descriptor failures and repeated failing paths sharing the same deduped key.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Composition",
    "description": "Register the DMS-982 resolver, relational access seam, and dialect-specific adapter seams in composition without wiring a new relational repository path. Acceptance: PostgreSQL service registration can resolve the prerequisite resolver and relational access seam when `UseRelationalBackend` is enabled, SQL Server tests can instantiate the SQL Server adapter through the same contract/factory seam, and no `IDocumentStoreRepository` request-path integration is added in this story.",
    "steps-to-verify": [
      "Add or update DI/composition tests that resolve the prerequisite resolver, relational access seam, and adapter factory from the configured service collection.",
      "Run: `dotnet test --no-restore ./src/dms/frontend/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit/EdFi.DataManagementService.Frontend.AspNetCore.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Shared Integration Fixtures",
    "description": "Add shared resolver integration fixtures and seed helpers for DMS-982 that are independent of the legacy JSON repository path and full E2E environment. Acceptance: shared helpers can seed `dms.ResourceKey`, `dms.Document`, `dms.ReferentialIdentity`, `dms.Descriptor`, and any required abstract identity tables/member tables from compact fixtures, and the helpers are reusable by both PostgreSQL and SQL Server resolver integration tests.",
    "steps-to-verify": [
      "Add shared test helpers that provision and seed compact resolver fixtures without depending on the old repository path.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Mssql.Tests.Integration/EdFi.DataManagementService.Backend.Mssql.Tests.Integration.csproj`"
    ],
    "completed": false
  },
  {
    "category": "PostgreSQL Harness Provisioning",
    "description": "Add PostgreSQL-specific provisioning and reset support for the focused DMS-982 integration harness. Acceptance: the PostgreSQL integration project can create the minimal resolver schema objects required by the shared fixtures, seed repeatable test datasets, and reset state between tests without relying on the full DMS E2E stack.",
    "steps-to-verify": [
      "Add PostgreSQL integration fixture setup that provisions the minimal resolver test database shape used by the shared fixtures.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": false
  },
  {
    "category": "SQL Server Harness Provisioning",
    "description": "Add SQL Server-specific provisioning and reset support for the focused DMS-982 integration harness. Acceptance: the SQL Server integration project can create the minimal resolver schema objects required by the shared fixtures, seed repeatable test datasets, and reset state between tests without relying on the full DMS E2E stack.",
    "steps-to-verify": [
      "Add SQL Server integration fixture setup that provisions the minimal resolver test database shape used by the shared fixtures.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Mssql.Tests.Integration/EdFi.DataManagementService.Backend.Mssql.Tests.Integration.csproj`"
    ],
    "completed": false
  },
  {
    "category": "PostgreSQL Integration",
    "description": "Add focused PostgreSQL integration coverage for the resolver service. Acceptance: tests cover request-local dedupe versus per-path diagnostics, missing document references at multiple concrete JSON locations, concrete-target mismatch, abstract/superclass alias resolution, descriptor membership failure, descriptor discriminator mismatch, and at least one large lookup batch that still resolves in bulk.",
    "steps-to-verify": [
      "Ensure the PostgreSQL integration test connection string is configured for `src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration`.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": false
  },
  {
    "category": "SQL Server Integration",
    "description": "Add focused SQL Server integration coverage for the resolver service with parity to PostgreSQL where the query strategy differs. Acceptance: tests cover the same success/failure matrix as PostgreSQL, include at least one threshold-crossing large-list case that exercises the SQL Server bulk strategy, and assert that missing and type-mismatch outcomes match PostgreSQL at the public-result level.",
    "steps-to-verify": [
      "Ensure the SQL Server admin connection string is configured for `src/dms/backend/EdFi.DataManagementService.Backend.Mssql.Tests.Integration`.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Mssql.Tests.Integration/EdFi.DataManagementService.Backend.Mssql.Tests.Integration.csproj`"
    ],
    "completed": false
  }
]
