[
  {
    "category": "Orchestration Contracts",
    "description": "Replace the DMS-983 terminal-stage seam with an executor-facing write request for DMS-984. Acceptance: `RelationalDocumentStoreRepository` selects the active `MappingSet`, `ResourceWritePlan`, and, for existing-document flows, the `ResourceReadPlan`, then hands one executor request into a new relational write executor service instead of calling a terminal stage directly. Introduce executor-facing contracts that carry the selected body, operation kind, target lookup result, mapping-set-scoped plans, trace metadata, and any executor-only diagnostics without forcing later runtime stages to rediscover them. `FlatteningInput`, `FlattenedWriteSet`, and the candidate row-buffer contracts remain usable by the executor, but `IRelationalWriteTerminalStage` is removed or demoted so it is no longer the architectural boundary for persistence orchestration.",
    "steps-to-verify": [
      "Add backend unit tests that prove the repository still enforces write-plan and read-plan guard rails, shapes the new executor request correctly, and no longer depends on `IRelationalWriteTerminalStage`.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Target Lookup Semantics",
    "description": "Replace the temporary PUT target-resolution seam with operation-correct lookup semantics. Acceptance: POST lookup returns either `CreateNew` or `ExistingDocument`; PUT lookup returns either `ExistingDocument` or `NotFound`; existing-document lookup includes `DocumentId`, `DocumentUuid`, and the observed `ContentVersion`; `RelationalWriteTargetContext` remains executor-facing and only models real write targets (`CreateNew` or `ExistingDocument`); `UpdateDocumentById` short-circuits missing PUT targets to `UpdateFailureNotExists` before reference resolution, flattening, current-state load, or executor execution; legacy behavior that converted missing PUT targets into `CreateNew` is removed from both production code and tests.",
    "steps-to-verify": [
      "Add unit tests for POST create-vs-update lookup, PUT existing-vs-not-found lookup, and repository short-circuit mapping to `UpdateFailureNotExists`.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Write Session",
    "description": "Introduce a request-scoped relational write session that owns one open connection and transaction per executor attempt. Acceptance: the write executor can use a shared connection and transaction for prerequisite lookups, current-state hydration, `CollectionItemId` reservation, and final DML on both PostgreSQL and SQL Server; commit and rollback are explicit and attempt-scoped; the old one-command-one-connection pattern no longer dictates write orchestration; low-level command executors may remain as utilities, but orchestration no longer depends on them as the primary seam; the session seam leaves room for future authorization statements to be prepended into the same roundtrip without another boundary redesign.",
    "steps-to-verify": [
      "Add backend unit tests for session lifecycle, commit and rollback behavior, and dialect selection for executor attempts.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Session-Oriented Reference Resolution",
    "description": "Move request-scoped reference resolution under the write executor so it can share the executor-owned session. Acceptance: the executor resolves document and descriptor references through the shared session without opening independent connections, preserves existing request-local memoization and reference-failure classification semantics, and short-circuits before flattening or DML when reference resolution fails. Reference failures continue to map to the current public `UpsertFailureReference` and `UpdateFailureReference` results.",
    "steps-to-verify": [
      "Add backend unit tests that prove the executor resolves references through the shared attempt boundary, preserves memoization behavior, and exits before flattening or DML on reference failures.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Executor-Owned Flattening",
    "description": "Move flattener invocation under the write executor after reference resolution succeeds. Acceptance: the repository no longer constructs `FlatteningInput` or `FlattenedWriteSet`; the executor builds `FlatteningInput` from the selected request body, resolved references, and executor-facing target context; the flattener contract remains pure request-body to row-buffer transformation with no session ownership; validation failures still short-circuit before any DML and continue to map to the current public validation results.",
    "steps-to-verify": [
      "Add backend unit tests that prove the executor constructs `FlatteningInput` only after successful reference resolution and that repository-level result mapping stays unchanged for flattener validation failures.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Current State Loader",
    "description": "Add a write-path-local, session-aware current-state loader for one existing document using the compiled read plan and `HydrationExecutor`. Acceptance: DMS-984 can load `DocumentMetadataRow` plus hydrated table rows for a single `DocumentId` through the executor-owned connection and transaction using `PageKeysetSpec.Single`; the loader is narrow and rowset-first, with no profile projection or JSON reconstitution requirements; public `IDocumentHydrator` stays unchanged for the read path; existing-document write attempts fail deterministically when a required `ResourceReadPlan` is missing; the loaded current state is reusable by both guarded no-op comparison and later DMS-1105 work; tests prove the guarded no-op path reuses this same current-state load rather than issuing a dedicated extra compare-only roundtrip.",
    "steps-to-verify": [
      "Add backend or plans tests that prove an existing-document write attempt can materialize current metadata plus table rows from the selected read plan without opening a second connection seam, and that guarded no-op preparation reuses this load instead of performing a second compare-only load.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "No-Profile Merge Synthesis",
    "description": "Implement the shared no-profile post-merge rowset synthesis helper for existing-document writes. Acceptance: PUT and POST-as-update use one shared helper for merge matching, deterministic sibling ordering, compare projection, and execution-ready rowset synthesis rather than separate compare-only logic; synthesis consumes `CollectionMergePlan.CompareBindingIndexesInOrder` and the same stable-identity metadata used by the real executor; the helper can project both non-collection scopes and collection scopes into the same stored and writable comparison space that later DML execution will use.",
    "steps-to-verify": [
      "Add backend and plans unit tests for compare-order reuse, deterministic sibling ordering, and shared rowset synthesis for non-collection and collection paths.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Identity Stability Guard Rails",
    "description": "Enforce the DMS-984 identity-stable boundary for existing-document writes. Acceptance: existing-document writes only proceed when the post-merge identity projection matches the stored identity projection; `allowIdentityUpdates=false` yields the normal immutable-identity failure path; resources that may eventually allow identity updates yield a deterministic relational not-yet-supported failure until the E09 identity-maintenance stories land; identity-changing cases are rejected before changed-write DML or guarded no-op success is returned.",
    "steps-to-verify": [
      "Add backend unit tests for identity-stable success, immutable-identity failure for `allowIdentityUpdates=false`, and deterministic not-yet-supported failure for `allowIdentityUpdates=true`.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Guarded No-Op And Freshness Recheck",
    "description": "Introduce internal executor attempt outcomes and implement the guarded no-op fast path for unchanged existing-document writes. Acceptance: the executor returns internal attempt results such as applied write, guarded no-op, and stale no-op compare rather than only public `UpsertResult` and `UpdateResult`; unchanged PUT and unchanged POST-as-update requests short-circuit as successful no-ops without issuing DML or bumping `dms.Document.ContentVersion`; the compare path reuses the same shared post-merge synthesis logic as execution; before returning success, the executor revalidates that the observed `ContentVersion` is still current for the target document; stale compares surface an internal stale outcome instead of returning success from stale state.",
    "steps-to-verify": [
      "Add backend unit tests for internal attempt outcome mapping, unchanged PUT, unchanged POST-as-update, no-DML short-circuiting, and stale compare classification after freshness recheck.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Stale No-Op Retry Outcomes",
    "description": "Implement repository-level stale-no-op retry handling on top of the executor's internal attempt outcomes. Acceptance: the repository maps successful attempts back to `InsertSuccess`, `UpdateSuccess`, or successful PUT responses; when a no-op compare goes stale and there is no external `If-Match` policy yet, the repository reruns exactly one full executor attempt against fresh state; if the retry also goes stale, the request maps to a write-conflict outcome rather than returning success from stale state; database exception classification and general retry-loop policy remain out of scope for this task.",
    "steps-to-verify": [
      "Add backend unit tests for stale-no-op classification, one-time retry behavior, and result mapping back to public repository outcomes.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Non-Collection Persistence",
    "description": "Implement transactional DML for `dms.Document`, the resource root row, root extension rows, and non-collection separate-table or inlined scopes on the no-profile path. Acceptance: create flows insert `dms.Document` and the required relational rows in dependency order; update flows update matched rows, insert newly present separate-table rows, delete omitted separate-table rows, and clear compiled bindings for omitted inlined scopes instead of preserving hidden data; no-profile root `_ext` rows and aligned one-to-one extension scopes follow the same present and absent semantics as base data; the executor commits all changed rows in one transaction and rolls back fully on failure with no partial writes left behind.",
    "steps-to-verify": [
      "Add backend unit tests for non-collection present and absent handling, inlined clear behavior, root extension row handling, and rollback-on-failure behavior using an injected failure hook.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Base Stable-Identity Collection Merge",
    "description": "Implement stable-identity merge execution for base collection and common-type tables on the no-profile path. Acceptance: the executor loads the current sibling set from the current-state loader, matches request candidates to stored rows by compiled semantic identity, updates matched rows in place, deletes omitted rows by stable row identity, reserves new `CollectionItemId` values only for unmatched inserts, and applies the same logic to both top-level and nested collection scopes. `Ordinal` recomputation uses the same deterministic post-merge ordering helper used by the guarded no-op compare path. `FullSurfaceCollectionReorder` at base collection scopes must prove matched rows keep their existing `CollectionItemId` values while only `Ordinal` changes.",
    "steps-to-verify": [
      "Add backend and plans unit tests for semantic-identity matching, matched-row preservation, delete-plus-insert on semantic identity changes, nested collection handling, and ordinal recomputation.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Extension Collection Merge",
    "description": "Extend stable-identity merge execution to no-profile extension child collections. Acceptance: root-level extension child collections and collection-aligned extension child collections use the same semantic-identity matching, matched-row preservation, delete, insert, `CollectionItemId` reservation, and ordinal recomputation rules as base collections; collection-aligned extension child collections remain keyed to the owning base row's stable identity rather than request ordinals; executor-side merge helpers stay shared instead of forking an extension-only implementation.",
    "steps-to-verify": [
      "Add backend and plans unit tests for root-level extension child collections, collection-aligned extension child collections, and stable ordering and identity preservation across those scopes.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Batching And Dialect Limits",
    "description": "Implement batched relational DML that respects dialect-specific parameter limits without introducing provider-specific bulk-loader paths in this story. Acceptance: inserts and updates are grouped to avoid N+1 execution patterns; multi-row insert chunking and parameter partitioning honor PostgreSQL and SQL Server parameter limits; update execution proves changed rows are applied through bounded grouped commands rather than one command per row; `CollectionItemId` reservation is batched by table or scope as needed; large collections that exceed one batch are persisted through multiple bounded batches inside the same transaction; provider-specific `COPY` and `SqlBulkCopy` optimizations remain out of scope for DMS-984.",
    "steps-to-verify": [
      "Add backend unit tests for batch partitioning decisions, parameter-limit handling, multi-batch collection insert behavior, and update-side batching or command-count behavior that would catch a per-row update fallback.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "SQL Server Contract Safety Net",
    "description": "Add the smaller SQL Server safety net this story needs without full SQL Server end-to-end integration. Acceptance: unit or contract tests cover SQL Server generated SQL shape, compare-order metadata consumption, batching and parameter partitioning behavior, and any dialect-specific `CollectionItemId` reservation assumptions touched by DMS-984; PostgreSQL remains the deep end-to-end gate for runtime behavior in this story.",
    "steps-to-verify": [
      "Add backend and plans unit or contract tests that exercise the DMS-984 SQL Server-specific SQL and batching paths.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Create Baseline",
    "description": "Add PostgreSQL integration coverage for the create half of the DMS-984 no-profile runtime baseline using compiled schema and plan fixtures rather than hand-written table definitions. Acceptance: integration tests cover `NoProfileWriteBehavior` POST create with nested collections, root `_ext`, collection `_ext`, and an extension child collection. Assertions are database-first and verify inserted rows, collection identities, extension persistence, and stored values after the write using at least one focused stable-key fixture.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that provision the compiled relational model, execute create flows through the repository and executor, and assert directly against database state for nested and `_ext` create scenarios.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Update Semantics",
    "description": "Add PostgreSQL integration coverage for the changed-update half of the DMS-984 no-profile runtime baseline. Acceptance: integration tests cover `NoProfileWriteBehavior` PUT update with one omitted separate-table scope and one omitted inlined scope proving delete-vs-clear semantics. Assertions are database-first and verify actual rows, present-vs-absent behavior, and stored values after update using a focused stable-key fixture.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that execute changed PUT flows through the repository and executor and assert directly against database state for omitted separate-table and omitted inlined-scope cases.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Post-As-Update And Authoritative Smoke",
    "description": "Add PostgreSQL integration coverage for POST-as-update on an existing document and a production-like compiled resource shape. Acceptance: integration tests cover `NoProfileWriteBehavior` POST-as-update on an existing document and include one authoritative DS 5.2 or extension-style smoke in addition to the focused fixtures already used elsewhere. Assertions are database-first and verify the public result type plus stored relational state after the write.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that execute POST-as-update flows through the repository and executor and assert directly against database state, including one authoritative production-like fixture smoke.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Guarded No-Op",
    "description": "Add PostgreSQL integration coverage for guarded no-op success. Acceptance: integration tests cover unchanged PUT guarded no-op and unchanged POST-as-update guarded no-op; unchanged writes prove unchanged rowsets and unchanged `dms.Document.ContentVersion`. Assertions remain database-first and verify both the public result type and the stored relational state.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that execute unchanged-write scenarios through the repository and executor and assert directly against row contents and `ContentVersion` behavior.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Full-Surface Collection Reorder",
    "description": "Add PostgreSQL integration coverage for `FullSurfaceCollectionReorder`. Acceptance: integration tests prove matched rows keep the same `CollectionItemId` values while only `Ordinal` changes under a no-profile full-surface reorder. Assertions are database-first and verify both the public result type and the stored relational state.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that execute full-surface reorder scenarios through the repository and executor and assert directly against `CollectionItemId` reuse, `Ordinal` values, and stored row contents.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Stale Compare Recheck",
    "description": "Add PostgreSQL integration coverage for stale compare handling. Acceptance: integration tests cover stale compare recheck with a concurrent content-version bump between compare and guarded short-circuit and prove stale no-op does not return success from stale state.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that execute stale-compare scenarios through the repository and executor and assert directly against final database state and public result mapping.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Multi-Batch Collections",
    "description": "Add PostgreSQL integration coverage for multi-batch writes. Acceptance: integration tests cover batch-threshold crossing with a collection large enough to require multiple bounded insert batches inside one transaction and prove the full collection persists correctly across batches.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that execute large-collection scenarios through the repository and executor and assert directly against stored collection rows after multi-batch persistence.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Rollback Safety",
    "description": "Add PostgreSQL integration coverage for transaction rollback. Acceptance: integration tests verify no partial rows commit when an injected failure occurs after an early write inside the executor transaction. Assertions are database-first and prove failed attempts leave no partial relational state behind.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that execute injected-failure scenarios through the repository and executor and assert directly against final database state after rollback.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Authoritative Fixture Harness",
    "description": "Add reusable PostgreSQL integration-test support for authoritative write fixtures so stronger production-shaped tests do not duplicate focused-fixture setup. Acceptance: the PostgreSQL write integration suite can provision authoritative `sample` and `ds-5.2` fixtures through one shared harness that compiles the mapping set, provisions generated DDL, configures `RelationalDocumentStoreRepository`, and exposes database-first helpers for stable document, collection, and `_ext` assertions without reintroducing hand-written table definitions.",
    "steps-to-verify": [
      "Refactor or add shared PostgreSQL integration test helpers that can stand up authoritative write-fixture scenarios for both `src/dms/backend/Fixtures/authoritative/sample` and `src/dms/backend/Fixtures/authoritative/ds-5.2`.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Authoritative Sample Write Smoke",
    "description": "Add end-to-end PostgreSQL write coverage against the authoritative sample fixture, not just DDL and compatibility-gate checks. Acceptance: integration tests execute repository and executor create and changed-update flows for at least one collection-bearing resource that uses sample extension tables, and assert database-first on persisted base rows, root `_ext`, collection-aligned `_ext`, extension child collections, stable `CollectionItemId` reuse, and `ContentVersion` changed-vs-unchanged behavior. The test resource should come from `src/dms/backend/Fixtures/authoritative/sample` rather than the focused stable-key fixtures.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that exercise authoritative sample create and PUT or POST-as-update flows through the real relational repository and assert directly against authoritative sample tables after each write.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "PostgreSQL Integration - Expanded Authoritative DS 5.2 Writes",
    "description": "Strengthen the existing authoritative DS 5.2 write smoke beyond the current root-only `SchoolYearType` POST-as-update case. Acceptance: PostgreSQL integration tests cover at least one additional authoritative DS 5.2 resource shape that exercises more of the DMS-984 executor surface, such as nontrivial root updates, collection merge behavior, guarded no-op, or omitted-scope semantics, while continuing to assert database-first on public result type, stored relational rows, `DocumentUuid` preservation, and `dms.Document.ContentVersion` behavior. The new tests should use `src/dms/backend/Fixtures/authoritative/ds-5.2` and avoid falling back to focused toy fixtures for the core write proof.",
    "steps-to-verify": [
      "Add PostgreSQL integration tests that expand authoritative DS 5.2 coverage to at least one additional write scenario and one additional resource shape beyond the current `SchoolYearType` post-as-update smoke.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Concurrency - Guarded No-Op Commit Window",
    "description": "Close the guarded no-op race where a concurrent writer can commit after the executor's freshness recheck but before the no-op transaction commits. Acceptance: unchanged PUT and POST-as-update do not return success from stale state under the shipped runtime isolation level; the compare plus freshness decision is protected either by executor-owned locking or a stronger transaction strategy that actually spans the no-op commit window; stale no-op outcomes still surface to the repository retry path instead of silently succeeding; verification includes a concurrency-focused PostgreSQL integration test that reproduces the pre-fix race and proves the fixed executor returns stale or retries rather than `UpdateSuccess`/`UpsertSuccess` from stale state.",
    "steps-to-verify": [
      "Add backend unit tests for the chosen locking or transaction strategy around the guarded no-op path and any changed freshness-check orchestration.",
      "Add PostgreSQL integration coverage that forces a concurrent write between no-op compare and commit and proves the request does not return success from stale state.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Executor Session Ownership - Existing Target Lookup",
    "description": "Finish collapsing the DMS-983 seam by moving existing-document target lookup for write attempts under the executor-owned session boundary. Acceptance: existing PUT and POST-as-update attempts resolve the target document, observe `ContentVersion`, and load current state on the same request-scoped connection and transaction rather than doing target lookup in the repository on a separate connection; concurrent delete or disappearance between lookup and hydration no longer leaks as an internal `InvalidOperationException`; missing existing targets map deterministically to the correct public outcomes (`UpdateFailureNotExists` for PUT, create-vs-update re-evaluation or conflict-safe handling for POST as appropriate); repository orchestration remains responsible only for mapping-set and plan guard rails plus one stale no-op retry.",
    "steps-to-verify": [
      "Add backend unit tests that prove repository orchestration no longer performs existing-document target lookup on a separate seam for executor-owned existing-document attempts.",
      "Add backend or integration coverage for concurrent-delete or target-disappeared scenarios so the path returns the intended public outcome instead of a 500-style internal failure.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Executor Cleanup - Remove Dead Persist Fallback",
    "description": "Remove the dead executor fallback branch left over from the old terminal-stage seam. Acceptance: `IRelationalWriteNonCollectionPersister` no longer returns a boolean that implies persistence may be 'not implemented' after real DML orchestration has started; `DefaultRelationalWriteExecutor` no longer carries an unreachable `UnknownFailure` branch gated on `TryPersistAsync` returning `false`; the persister either succeeds or throws a meaningful exception, and the surrounding contracts, tests, and helper messages are simplified so the write path no longer advertises an impossible partial-implementation outcome.",
    "steps-to-verify": [
      "Refactor the persister and executor contracts to eliminate the boolean fallback path and update unit tests to assert the simplified success and failure behavior.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Dependency Docs - E13-S01 Summary Row",
    "description": "Fix the stale epic-level summary row for `E13-S01` in `reference/design/backend-redesign/epics/DEPENDENCIES.md` so it matches the detailed hard-dependency table. Acceptance: the E13 summary table lists the same hard dependencies already recorded in the detailed table for `E13-S01`, namely `E07-S03`, `E07-S03b`, `E07-S05b`, `E08-S01`, and `E11-S01`, and the dependency document no longer presents conflicting dependency sets for that story.",
    "steps-to-verify": [
      "Update the E13 summary row in `reference/design/backend-redesign/epics/DEPENDENCIES.md` and confirm it matches the detailed hard-dependency entries for `E13-S01` later in the same document."
    ],
    "completed": true
  },
  {
    "category": "Code Review Follow-Up - SQL Server Current-State Scalar Normalization",
    "description": "Fix the SQL Server no-profile compare and merge regression caused by current-state hydration preserving raw ADO values instead of normalizing them into the same storage-space types the write flattener emits. Today `HydrationReader.ReadTableRowsAsync(...)` stores `DbDataReader.GetValue(...)` results verbatim, `RelationalWriteNoProfileMergeSynthesizer.CurrentStateProjection.ProjectCurrentRows(...)` wraps those values directly in `FlattenedWriteValue.Literal`, and the flattener emits normalized `DateOnly` and `TimeOnly` values for `ScalarKind.Date` and `ScalarKind.Time`. On SQL Server, `date` and `time` materialize as `DateTime` and `TimeSpan`, so unchanged rows can compare unequal, guarded no-op can fall through to unnecessary DML, and collection semantic-identity matching can miss unchanged rows and degrade into delete plus insert behavior. Acceptance: current-state projection normalizes hydrated values per compiled scalar type before compare and match logic runs, unchanged SQL Server writes with `date` and `time` values succeed through the guarded no-op path, and collection merge matching uses the same normalized value space on both PostgreSQL and SQL Server.",
    "steps-to-verify": [
      "Add backend unit tests that hydrate SQL Server-shaped `date` and `time` values (`DateTime` and `TimeSpan`) and prove `RelationalWriteNoProfileMergeSynthesizer` produces the same comparable and semantic-identity values as the flattener (`DateOnly` and `TimeOnly`).",
      "Add SQL Server contract tests that prove unchanged existing-document writes with `date` and `time` bindings classify as guarded no-op instead of changed-write.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Code Review Follow-Up - PostgreSQL Re-Evaluation Hydration Temp Table Reuse",
    "description": "Fix the PostgreSQL executor path where POST existing-target re-evaluation can invoke current-state hydration twice on the same write session and transaction. The executor now owns target lookup and may call `RelationalWriteCurrentStateLoader.LoadAsync(...)`, then on a missing current state re-run POST lookup and hydrate again inside the same session. The hydration batch still unconditionally emits `CREATE TEMP TABLE page ... ON COMMIT DROP`, and the keyset table name is fixed by `KeysetTableConventions`, so the second hydration call on the same PostgreSQL transaction can fail with `relation \"page\" already exists` instead of returning the intended create or write-conflict outcome. Acceptance: repeated current-state hydration within one executor-owned PostgreSQL session is safe, either because the keyset temp table is dropped or truncated between runs or because the hydration batch uses a session-safe temp-table strategy; POST re-evaluation paths no longer fail with temp-table name collisions.",
    "steps-to-verify": [
      "Add backend or plans tests that execute two PostgreSQL hydration batches on the same connection and transaction and prove the second batch succeeds.",
      "Add executor coverage for the POST existing-target-disappeared re-evaluation path using the real PostgreSQL hydration executor or a close contract test so the path returns create or conflict rather than a temp-table collision.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Code Review Follow-Up - Restore Repository-Orchestrated Target Lookup Boundary",
    "description": "Restore the story's intended repository-orchestrated target-lookup boundary instead of leaving target lookup inside the executor. The current branch has the repository build a `RelationalWriteTargetRequest` and always invoke the executor, which opens the write session and performs lookup there. That preserves public behavior for now, but it backs away from the agreed DMS-984 seam: repository-facing lookup results should distinguish POST create-vs-existing and PUT not-found before executor entry, and `RelationalWriteTargetContext` should remain executor-facing for real write targets only. Acceptance: `RelationalDocumentStoreRepository` performs repository-facing target lookup, short-circuits missing PUTs before executor/session/reference-resolution work begins, passes the resolved lookup result or executor-facing target context into the executor request, and the executor owns current-state load, compare, merge, and persistence without also owning operation-correct lookup policy.",
    "steps-to-verify": [
      "Refactor repository and executor contracts so repository orchestration resolves the target before invoking the executor and only real write targets enter executor persistence flow.",
      "Add backend unit tests that prove missing PUT targets return `UpdateFailureNotExists` without opening executor-owned write-session work and that POST create-vs-existing lookup still maps correctly.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Code Review Follow-Up - Remove Duplicate Repository Validation Mapping",
    "description": "Remove the now-dead validation-exception mapping left in `RelationalDocumentStoreRepository.ExecuteWriteGuardRails(...)`. `DefaultRelationalWriteExecutor.ExecuteAsync(...)` already catches `RelationalWriteRequestValidationException` and maps it to the public validation result, so the repository-level catch block is redundant as long as production uses the default executor. Acceptance: validation mapping lives in one place, repository orchestration no longer carries an unreachable duplicate catch path, and related tests are simplified to the chosen single owner of validation-result translation.",
    "steps-to-verify": [
      "Delete or consolidate the duplicate validation-exception handling path and update backend unit tests so they assert a single owner for validation-result translation.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Code Review Follow-Up - Reopen SQL Server Current-State Scalar Normalization",
    "description": "Finish the still-open SQL Server no-profile compare and merge fix. Acceptance: current-state projection no longer wraps raw hydrated ADO values directly into `FlattenedWriteValue.Literal` for compare and semantic-identity matching; instead it normalizes hydrated values into the same storage-space types the write flattener emits, including `DateOnly` for `ScalarKind.Date` and `TimeOnly` for `ScalarKind.Time`; unchanged existing-document SQL Server writes with `date` and `time` bindings classify as guarded no-op instead of changed-write; collection semantic-identity matching uses the same normalized value space on both PostgreSQL and SQL Server so unchanged collection rows do not degrade into delete plus insert behavior.",
    "steps-to-verify": [
      "Add backend unit tests that hydrate SQL Server-shaped `date` and `time` values (`DateTime` and `TimeSpan`) and prove merge synthesis produces the same comparable and semantic-identity values as the flattener (`DateOnly` and `TimeOnly`).",
      "Add backend unit or contract coverage that proves unchanged existing-document writes with SQL Server `date` and `time` bindings return the guarded no-op path instead of falling through to persistence.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Plans.Tests.Unit/EdFi.DataManagementService.Backend.Plans.Tests.Unit.csproj`"
    ],
    "completed": true
  },
  {
    "category": "Code Review Follow-Up - Batch Collection-Aligned Extension Scope Persistence",
    "description": "Remove the remaining N+1 persistence path for collection-aligned extension scopes. Acceptance: `DbTableKind.CollectionExtensionScope` rows no longer execute one insert, update, or delete command per row when large no-profile writes touch collection-aligned `_ext` scopes; collection-aligned scope persistence uses bounded batch execution that respects the compiled dialect batching limits and parameter ceilings already carried on the table plans; the batched path preserves existing behavior for `BaseCollectionItemId`, newly reserved `CollectionItemId` values, and matched-row update semantics; no-profile writes with large collection-aligned `_ext` payloads stay inside one transaction without falling back to per-row DML.",
    "steps-to-verify": [
      "Refactor collection-aligned extension scope delete and upsert paths to batch commands using the same bounded command-count rules expected of the rest of DMS-984 persistence.",
      "Add backend unit tests that cover multi-row insert, update, and delete behavior for collection-aligned extension scopes and would fail on a one-command-per-row fallback.",
      "Add PostgreSQL integration coverage for a collection-aligned `_ext` write large enough to cross one batch boundary and assert both persisted row state and bounded command execution.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration/EdFi.DataManagementService.Backend.Postgresql.Tests.Integration.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Code Review Follow-Up - Remove Dead Session PUT Lookup Surface",
    "description": "Delete the unused session-side PUT lookup path left behind after restoring repository-owned target lookup. Acceptance: production write orchestration no longer exposes or carries an executor-session `ResolveForPutAsync(...)` surface that nothing in the runtime uses; the repository remains the only owner of PUT not-found lookup policy; executor recovery keeps only the session-side POST re-evaluation path needed after current-state load misses; contracts, registrations, and tests are simplified so the codebase no longer suggests that PUT lookup can occur on both repository and executor seams.",
    "steps-to-verify": [
      "Remove the unused session PUT lookup entrypoint and update runtime wiring so only the actually used lookup surfaces remain.",
      "Add or update backend unit tests to prove PUT not-found short-circuiting still occurs at the repository boundary and POST existing-target re-evaluation still works inside the executor session.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  },
  {
    "category": "Code Review Follow-Up - Deduplicate Session Command Construction",
    "description": "Simplify the duplicated session command-building logic shared by `RelationalWriteSession.CreateCommand(...)` and `SessionRelationalCommandExecutor.ExecuteReaderAsync(...)`. Acceptance: one shared helper owns session-bound `DbCommand` creation and parameter binding for `RelationalCommand`; both the write-session and session command-executor paths use that shared helper instead of keeping parallel parameter loops; behavior stays unchanged for command text, transaction binding, null handling, and optional parameter configuration; the codebase no longer has two independent implementations that can drift apart.",
    "steps-to-verify": [
      "Extract the common session command creation and parameter-binding logic into one shared helper and update both call sites to use it.",
      "Add or update backend unit tests that cover command text, transaction assignment, parameter naming, null handling, and parameter configurator behavior through both call sites.",
      "Run: `dotnet test --no-restore ./src/dms/backend/EdFi.DataManagementService.Backend.Tests.Unit/EdFi.DataManagementService.Backend.Tests.Unit.csproj`"
    ],
    "completed": false
  }
]
