
CREATE TABLE IF NOT EXISTS "{{tableName}}" (
{{#each columns}}
    "{{name}}" {{type}}{{#if isRequired}} NOT NULL{{/if}}{{#unless @last}},{{/unless}}
{{/each}}
{{#each pkColumns}}
{{#if @first}}
{{#if ../columns}},{{/if}}
    PRIMARY KEY ({{/if}}"{{this}}"{{#unless @last}}, {{else}}){{/unless}}
{{/each}}
{{#each fkColumns}},
    FOREIGN KEY ("{{column}}") REFERENCES "{{parentTable}}"("{{parentColumn}}")
{{/each}}
);
