# Ed-Fi API Identity Contract

This is the Ed-Fi API Identity contract package.
It publishes the interfaces and types an implementer uses to provide Identity Management behavior for the Ed-Fi Data Management Service.
The implementer guide arrives in a later story.
